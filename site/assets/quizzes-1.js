/* ===========================================================================
   quizzes-1.js — the question bank for chapter 00 and Parts 1 and 2.

   THE ONE RULE THAT MATTERS: question ids are "<chapter-id>#<index>" and the
   index IS the array position. That id is the key a learner's spaced-repetition
   schedule is stored under.

        APPEND ONLY.  NEVER REORDER.  NEVER DELETE.

   Reordering silently reassigns other people's review history to the wrong
   questions — their schedule keeps working and is now about something else.
   tools/quiz-ids.lock exists specifically to make that a build failure.

   Other rules, all deliberate:
   - `why` is shown on CORRECT answers too. Feedback that only appears on
     failure teaches people to guess and check.
   - Every stem must stand alone two weeks later, out of context, because each
     question is also a flashcard. "Which of these is true?" fails that test.
   - `<` and `>` must be escaped in `q` and in `a` (rendered as HTML).
     They must NOT be escaped in `code` (rendered as text).
   =========================================================================== */

window.QUIZZES = Object.assign(window.QUIZZES || {}, {

  /* ------------------------------------------------ 00 · the job posting -- */
  "00-the-job-posting": [
    { q: "An Italian advert says <em>“ottimizzazione e tuning delle query”</em>. What is the team most likely telling you about their situation?",
      a: ["They want someone to design a new database from scratch",
          "Something is already slow and nobody on the team can read an execution plan",
          "They need a DBA to manage backups",
          "They are migrating to a new engine"],
      c: 1,
      why: "It is the single most common reason a SQL-focused role is opened. The system works; it is too slow; the people who built it write application code and cannot diagnose the database half." },

    { q: "The advert asks for <em>“3 anni di esperienza su ambienti di produzione”</em> and you have none. What is the strongest response?",
      a: ["Apply anyway and hope it is not checked",
          "Do not apply — the requirement is absolute",
          "Name the specific production concerns you can demonstrate, and be explicit about what you have not yet lived through",
          "Claim personal projects count as production"],
      c: 2,
      why: "Years are a proxy for exposure to deadlocks, failed restores and locking migrations. You can demonstrate the knowledge and be honest about the missing scar tissue. Hedging vaguely is what loses the room, not the gap itself." },

    { q: "Which three responsibilities does essentially every SQL developer posting reduce to?",
      a: ["Modelling, reporting, documentation",
          "Read paths, write paths, operability",
          "SELECT, INSERT, DELETE",
          "Design, development, testing"],
      c: 1,
      why: "Retrieve correct data fast enough (read paths); change it without violating invariants or racing (write paths); know when the system is unhealthy and be able to restore it (operability). Everything in the course is one of the three." },

    { q: "An advert names SQL Server, Oracle <em>and</em> MongoDB. What does that combination usually mean in practice?",
      a: ["You must be expert in all three",
          "The company runs several systems of different ages and wants someone who is not frightened by any of them",
          "It is a mistake in the advert",
          "MongoDB is the main system"],
      c: 1,
      why: "Multi-engine adverts describe an estate, not a person. The right answer names your depth in one and your working familiarity with the others, with the level attached to each." },

    { q: "Why does this course declare <code>req</code> or <code>extra</code> on every chapter?",
      a: ["To sort the sidebar",
          "So the claim that nothing in the adverts is uncovered is checkable rather than asserted",
          "To decide which chapters are optional",
          "For search-engine keywords"],
      c: 1,
      why: "Both coverage tables on the home page are generated from the same manifest that builds the sidebar. A chapter cannot exist without declaring which advert line it answers, or admitting that none does and saying why it is there anyway." },

    { q: "Asked “what is the difference between WHERE and HAVING?”, which answer is strongest?",
      a: ["HAVING filters groups",
          "HAVING is used with GROUP BY",
          "WHERE filters rows before grouping so it can use an index and reduces what the aggregate reads; HAVING filters groups afterwards",
          "They are interchangeable"],
      c: 2,
      why: "The first is correct and stops at the syntax. The third adds why it matters, which is the half of the answer interviewers are actually listening for." },

    { q: "Which engine is the safest single choice to learn deeply for the Italian market?",
      a: ["It does not matter much — learn the standard deeply plus one dialect fluently",
          "Only PostgreSQL",
          "Only MongoDB",
          "Whichever is newest"],
      c: 0,
      why: "About 85% of what you write is portable. The dialect differences are a lookup table you internalise in a week. Depth in one engine plus standard fluency beats shallow familiarity with four." },

    { q: "An advert asks for “sviluppo di stored procedure, viste, funzioni e trigger”. What does that tell you about the codebase?",
      a: ["It is a greenfield project",
          "Business logic already lives inside the database and you will maintain it",
          "They use an ORM exclusively",
          "They want you to remove the stored procedures"],
      c: 1,
      why: "Whether or not putting logic in the database was a good idea, it is there, and the job includes it. Arriving with an opinion about that and no willingness to work in it is a poor start." },

    { q: "You are asked “why databases rather than application code?”. Which answer is best?",
      a: ["I find front-end work boring",
          "Databases pay better",
          "The data outlives every application built on it, so the decisions here are the expensive ones — and correctness is checkable, because a constraint either holds or it does not",
          "I am not good at JavaScript"],
      c: 2,
      why: "A positive, specific reason that shows you understand the layer's role. Answers framed as avoiding something else invite the follow-up “so what happens when we need you on the API?”." },

    { q: "The advert mentions “esperienza nello sviluppo ETL con SSIS”. What technical problem does that job actually involve most often?",
      a: ["Writing complex SELECT statements",
          "Making a nightly load idempotent, so re-running it does not duplicate data",
          "Designing star schemas",
          "Configuring the SSIS server"],
      c: 1,
      why: "Data arrives from systems you do not control, in a shape that changes, and jobs get re-run after failures. Idempotency — upsert on a business key, watermarks — is the daily problem." },

    { q: "Which of these is the honest description of the coverage tables on this site's home page?",
      a: ["A guarantee you will pass any interview",
          "A statement that every line of the collected adverts has a chapter, and that ten chapters exist for reasons no advert stated",
          "A ranking of chapter importance",
          "A list of what to skip"],
      c: 1,
      why: "It is a coverage claim about the source material, nothing more. It says nothing about whether you have learned it — that is what the mastery percentage is for, and even that is only about these questions." }
  ],

  /* ------------------------------------------ 01 · engines and storage --- */
  "01-engines-and-storage": [
    { q: "What is the smallest unit of data a relational engine reads from or writes to disk?",
      a: ["A row", "A page (typically 8 KB, or 16 KB in InnoDB)", "A byte", "A table"],
      c: 1,
      why: "Never one row. This single fact explains row width, index design, and why SELECT * costs more than the extra bytes suggest." },

    { q: "What must reach durable storage before a COMMIT can return?",
      a: ["Every modified data page",
          "The write-ahead log records for the transaction",
          "The whole table",
          "Nothing — commits are asynchronous"],
      c: 1,
      why: "Write-ahead logging: the log is flushed at commit, the dirty data pages are written later by a checkpoint. Appending to a log is sequential and cheap; rewriting scattered pages is not. That is what makes commits fast and crash recovery possible." },

    { q: "A table's rows are 800 bytes wide instead of 100. Roughly how does a full scan change on an 8 KB page size?",
      a: ["No change — the same rows are read",
          "Roughly eight times as many page reads",
          "Roughly eight times fewer page reads",
          "It depends only on the indexes"],
      c: 1,
      why: "About 80 rows fit per page at 100 bytes and about 10 at 800. Same rows, eight times the pages. Row width is a performance property, not just a storage one." },

    { q: "Where does a very large column value (TEXT, VARCHAR(MAX), a big JSONB document) normally live?",
      a: ["Inline in the row, always",
          "Off-page, with a pointer left in the row (LOB storage / TOAST)",
          "In the transaction log",
          "In a separate database"],
      c: 1,
      why: "Reading it costs an extra jump; not selecting it costs nothing. This is one of the concrete reasons SELECT * can be far worse than naming columns." },

    { q: "What is a page split, and why does it matter later?",
      a: ["The engine splitting a query across CPUs",
          "An update that no longer fits forces the page into two half-empty pages, so the same data now occupies more pages forever",
          "Splitting a table across files",
          "A partitioning operation"],
      c: 1,
      why: "That is fragmentation. More pages for the same data means more reads on every subsequent scan, and it is why a fill factor and a sequential clustering key matter." },

    { q: "In an MVCC engine such as PostgreSQL or Oracle, what does a reader do when a writer is changing the row it wants?",
      a: ["Waits for the writer to commit",
          "Reads a previous version of the row and does not block",
          "Gets an error",
          "Reads the uncommitted value"],
      c: 1,
      why: "Multi-version concurrency control: readers see a consistent snapshot without blocking writers. The cost is that old versions accumulate and must be cleaned up — VACUUM, undo segments, the InnoDB purge thread." },

    { q: "A PostgreSQL table holding about 2 GB of live data occupies 100 GB on disk. What is the most likely cause?",
      a: ["The indexes are too large",
          "Table bloat: dead row versions that VACUUM could not remove, usually because a long-running transaction is holding back the horizon",
          "The page size is misconfigured",
          "Compression is disabled"],
      c: 1,
      why: "MVCC keeps old versions until nothing can still see them. One forgotten open transaction pins that horizon and bloat accumulates behind it. Look for long-running transactions before anything else." },

    { q: "In SQL Server, “the transaction log is full” in a database using the FULL recovery model most often means what?",
      a: ["The disk is genuinely out of space and needs growing",
          "Nobody is taking log backups, so the log cannot truncate",
          "Too many indexes",
          "The buffer pool is too small"],
      c: 1,
      why: "FULL recovery keeps log records until a log backup is taken. Without one the log grows until the volume fills. The fix is a log backup schedule — or SIMPLE recovery, if point-in-time restore genuinely is not required." },

    { q: "Roughly how much slower is reading a page from a local SSD than reading one already in the buffer pool?",
      a: ["About 10 times", "About 1 000 times", "About the same", "About 10 million times"],
      c: 1,
      why: "Roughly 100 ns versus roughly 100 µs. Spinning disk is another hundred times worse again. Almost all tuning is an argument about keeping the working set in memory." },

    { q: "A thousand individually fast queries, each costing a 0.5 ms network round trip, is a well-known problem. Why is it hard to spot?",
      a: ["It shows up as one very slow query",
          "Every individual query is fast, so no slow-query log or average-duration report flags it",
          "It only happens under load",
          "The database logs it as an error"],
      c: 1,
      why: "The N+1 problem. Sorting queries by total duration rather than average is what makes it visible, which is why chapter 42 insists on that ordering." },

    { q: "Which statement about SQLite is accurate?",
      a: ["It is not a real transactional SQL engine",
          "It is fully transactional and standards-compliant, but single-writer, so it is a poor fit for concurrent write workloads",
          "It supports unlimited concurrent writers",
          "It cannot enforce foreign keys at all"],
      c: 1,
      why: "SQLite is a serious engine — it runs this site's playground and its accounts service. Its real limitation is write concurrency, not correctness. (Foreign keys work; they just need PRAGMA foreign_keys=ON.)" },

    { q: "What does the query optimiser actually do?",
      a: ["Rewrites your SQL into better SQL",
          "Chooses an execution plan — join order, join algorithms, access methods — using statistics about the data",
          "Caches query results",
          "Compresses data pages"],
      c: 1,
      why: "SQL says what, not how. The optimiser decides how, and it decides using estimates. That is why bad statistics produce bad plans, and why chapter 28 exists." }
  ],

  /* --------------------------------------------- 02 · relational model --- */
  "02-relational-model": [
    { q: "A query returns rows in a helpful order during testing and a different order in production. What is the explanation?",
      a: ["The data changed",
          "There is no ORDER BY, so the engine may return rows in any order it likes and its choice of plan changed",
          "The indexes are corrupted",
          "Production uses a different collation"],
      c: 1,
      why: "A table is an unordered set. An order you did not ask for is an accident of the plan — a parallel scan, a different index, updated statistics — and it will stop happening without warning." },

    { q: "What makes a candidate key <em>candidate</em>?",
      a: ["It is the fastest key to search",
          "It is unique and MINIMAL — remove any column and it stops being unique",
          "It was chosen as the primary key",
          "It is an integer"],
      c: 1,
      why: "Minimality is the whole point. If (email) is unique then (email, id) is unique too, but it is not a candidate key because a column can be removed without losing uniqueness." },

    { q: "What is the recommended approach when a table has both a meaningful natural key and a surrogate id?",
      a: ["Use only the natural key",
          "Use only the surrogate key",
          "Surrogate as the primary key, plus a UNIQUE constraint on the natural key",
          "Use a composite of both as the primary key"],
      c: 2,
      why: "You get stable narrow foreign keys and the business rule still enforced. The common failure is a surrogate id with no unique constraint on the natural key — which is how you end up with two rows for one customer." },

    { q: "In most engines, how many NULL values may a UNIQUE constraint permit in a column?",
      a: ["None",
          "Exactly one, in every engine",
          "Many — because two NULLs are not equal — except in SQL Server, which allows only one",
          "Unlimited, in every engine"],
      c: 2,
      why: "A genuine portability trap. PostgreSQL, Oracle and MySQL allow many; SQL Server allows one. For “at most one active row per customer”, use a filtered/partial unique index rather than relying on null semantics." },

    { q: "How is a many-to-many relationship represented relationally?",
      a: ["A comma-separated column",
          "Two foreign keys on one of the tables",
          "A third table holding a foreign key to each side",
          "It cannot be represented"],
      c: 2,
      why: "A junction table. And it is an entity in its own right — order_lines carries quantity and the price at the time of sale, neither of which belongs on products or on orders." },

    { q: "Why does an order line store the unit price rather than reading it from the product table?",
      a: ["For speed",
          "Because it is a different fact: the price charged that day, which must not change when the price list does",
          "Because products may be deleted",
          "It is a normalisation mistake"],
      c: 1,
      why: "products.list_price is the current price; order_lines.unit_price is what was charged. Normalising it away means reprinting last year's invoice silently reprices it." },

    { q: "Why is <code>WHERE x = NULL</code> never true, even for rows where x is null?",
      a: ["It is a syntax error",
          "NULL means unknown, so the comparison yields UNKNOWN, and WHERE keeps only rows where the predicate is TRUE",
          "NULL is stored as an empty string",
          "It works in some engines"],
      c: 1,
      why: "Three-valued logic. Use IS NULL, or IS NOT DISTINCT FROM where the dialect supports it." },

    { q: "A column should be declared NOT NULL unless what is true?",
      a: ["It is a foreign key",
          "There is a genuine business state in which the value is unknown or inapplicable",
          "It is indexed",
          "It is a string"],
      c: 1,
      why: "Nullable-by-default imports three-valued logic into every query touching the column. Adding NOT NULL later means cleaning up existing violations, which is a migration and an argument." },

    { q: "What does a foreign key constraint guarantee?",
      a: ["The child column is indexed",
          "Every non-null value in the child column exists as a key value in the parent table",
          "Deletes cascade automatically",
          "The two tables are stored together"],
      c: 1,
      why: "Referential integrity, nothing more. In particular it does NOT create an index on the child column in most engines — which is why deletes on the parent can suddenly scan the child table." },

    { q: "If the position of a row within a group is part of the business meaning, how should it be stored?",
      a: ["Rely on insertion order",
          "As an explicit column, such as line_no",
          "Use the physical row id",
          "Use the primary key sequence"],
      c: 1,
      why: "A relation has no order. Insertion order and physical row ids are storage artefacts that change under maintenance, reorganisation or a different plan." },

    { q: "Which of these is the difference between a primary key and a unique constraint?",
      a: ["There is none",
          "A primary key is implicitly NOT NULL, there is one per table, and it usually determines physical order; a unique constraint permits nulls and you may have many",
          "A unique constraint is faster",
          "A primary key cannot be composite"],
      c: 1,
      why: "The physical-order clause is the part candidates usually miss, and it is what makes primary key width matter for every other index on the table." }
  ],

  /* ---------------------------------------------------- 03 · data types -- */
  "03-data-types": [
    { q: "Which type should store a monetary amount?",
      a: ["FLOAT", "REAL", "DECIMAL(p, s) or an integer number of minor units", "DOUBLE PRECISION"],
      c: 2,
      why: "Binary floating point cannot represent decimal fractions exactly, so errors accumulate and equality comparisons become unreliable. Exact decimal or integer cents." },

    { q: "Why is SQL Server's MONEY type a poor choice for currency despite the name?",
      a: ["It cannot store negative values",
          "It is a scaled integer that truncates intermediate results, so dividing then multiplying loses precision",
          "It is deprecated and will be removed",
          "It only supports US dollars"],
      c: 1,
      why: "Microsoft's own guidance is to prefer DECIMAL. Knowing this is a genuine differentiator in a SQL Server interview." },

    { q: "What does collation control?",
      a: ["Only the display format of strings",
          "Sort order AND equality — so it affects GROUP BY, UNIQUE and which rows come back",
          "Only the sort order",
          "The character encoding only"],
      c: 1,
      why: "Under a case-insensitive collation 'Rossi' = 'rossi' is true, so a UNIQUE constraint rejects the second. It is a correctness setting, not a formatting one." },

    { q: "Two tables are joined on VARCHAR columns with different collations. What is the likely performance consequence?",
      a: ["None",
          "An implicit conversion that prevents an index seek on the join column",
          "The join fails with an error in every engine",
          "The result set is wrong"],
      c: 1,
      why: "Collation mismatch forces a conversion, and a converted column cannot be seeked. Some engines error instead; the slow-but-correct case is the harder one to notice." },

    { q: "An events table uses a random UUID (v4) as its clustered primary key and inserts have become very slow at scale. Why?",
      a: ["UUIDs are too large to index",
          "Each insert lands on a random page, so the write working set becomes the whole table and pages split constantly",
          "UUID generation is slow",
          "UUIDs cannot be indexed"],
      c: 1,
      why: "Insertion locality. UUID v7 or NEWSEQUENTIALID() are time-ordered, so inserts stay at the end of the index — same identifier benefits, none of the scatter." },

    { q: "Why does the width of the clustering key affect every other index on the table?",
      a: ["It does not",
          "Because every non-clustered index stores the clustering key as its row locator, so a wide clustering key makes all of them wider",
          "Because indexes are stored in the same pages",
          "Because of statistics"],
      c: 1,
      why: "A 16-byte UUID clustering key adds 8 bytes per entry to every other index compared with a BIGINT. On a table with six indexes that is a real amount of memory and I/O." },

    { q: "Which type is right for a timestamp recording when something actually happened, in a system used in several countries?",
      a: ["A local DATETIME with no zone",
          "A zone-aware type, or UTC (timestamptz, DATETIMEOFFSET)",
          "A string in the user's format",
          "A DATE"],
      c: 1,
      why: "An instant needs a zone or a fixed reference. Local time is ambiguous for one hour every autumn when the clocks go back, and rows recorded in that hour can never be ordered correctly." },

    { q: "You need to store a time zone with a future appointment. What should you store?",
      a: ["The UTC offset, e.g. +02:00",
          "The IANA zone name, e.g. Europe/Rome",
          "The country code",
          "The city name in free text"],
      c: 1,
      why: "An offset is a snapshot of a rule; a zone is the rule with its history. Rome is +01:00 in winter and +02:00 in summer, and DST rules have changed before and may again." },

    { q: "When is CHAR(n) the right choice over VARCHAR(n)?",
      a: ["When the column is indexed",
          "When every value genuinely is exactly n characters — a two-letter country code, a currency code",
          "When the column is a primary key",
          "Never"],
      c: 1,
      why: "CHAR is blank-padded to n. For variable-length data it wastes space and produces surprising trailing-space comparison behaviour." },

    { q: "Which is the best default way to represent an order status?",
      a: ["A bare integer whose meaning lives only in application code",
          "A lookup table with a foreign key, or a CHECK constraint on a short VARCHAR",
          "A boolean per status",
          "A JSON column"],
      c: 1,
      why: "The bare integer makes WHERE status = 3 unreadable in a report, unverifiable in a bug ticket, and unconstrained. A lookup table is self-documenting and joinable; a CHECK is readable in raw output." },

    { q: "What does a VARCHAR length limit actually do for you?",
      a: ["It reduces storage — shorter limits store fewer bytes",
          "It is a business constraint on what may be stored, not a storage optimisation",
          "It speeds up index seeks",
          "Nothing at all"],
      c: 1,
      why: "Storage is by actual length in every mainstream engine. The limit exists to reject data that is wrong — a 4000-character surname is a bug, not a long name." }
  ],

  /* -------------------------------------------- 04 · DDL and constraints -- */
  "04-ddl-and-constraints": [
    { q: "Why enforce an invariant with a database constraint rather than in application code?",
      a: ["Constraints are faster",
          "Because the constraint holds for every writer — the second application, the import job, the hotfix, the DBA at 2am — and it is also true for data already present",
          "Application validation is not possible",
          "It reduces the amount of code"],
      c: 1,
      why: "Application validation is the same rule written somewhere it can be bypassed. Over a system's life it will be bypassed." },

    { q: "Deleting a row from a parent table has become very slow and holds locks. What is the most likely cause?",
      a: ["The parent table is too large",
          "A child table has an unindexed foreign key column, so the engine scans it to prove nothing references the row",
          "Too many indexes on the parent",
          "The primary key is a UUID"],
      c: 1,
      why: "A foreign key does not create an index on the child column in any major engine except MySQL. This is one of the most common causes of mysterious delete-and-blocking incidents." },

    { q: "Which ON DELETE action should be the default for a foreign key on financial or legal records?",
      a: ["CASCADE", "SET NULL", "NO ACTION / RESTRICT", "SET DEFAULT"],
      c: 2,
      why: "Refusing the delete forces the caller to say what they meant. CASCADE lets one statement destroy rows in tables it does not name, several levels deep, with a row count that gives no clue." },

    { q: "Why should every constraint be given an explicit name?",
      a: ["It is required by the standard",
          "Generated names differ per environment, so migrations that drop them are not portable and error messages name nothing meaningful",
          "Named constraints are faster",
          "It allows more constraints per table"],
      c: 1,
      why: "An error saying ck_orders_total_nonneg is a sentence. One saying CK__orders__7A672E12 costs a support engineer twenty minutes." },

    { q: "In SQL Server, what is the practical consequence of a constraint being marked <em>untrusted</em>?",
      a: ["It stops being enforced",
          "It is still enforced, but the optimiser will no longer use it to simplify plans",
          "It is dropped at the next restart",
          "It applies only to new rows"],
      c: 1,
      why: "Typically caused by adding it WITH NOCHECK, often after a bulk load. Enforcement continues; the plan quality quietly degrades. Check sys.check_constraints.is_not_trusted." },

    { q: "How does the optimiser use a CHECK constraint?",
      a: ["It does not — constraints only validate writes",
          "As information: it can discard branches querying for values the constraint proves cannot exist",
          "To decide index order",
          "To compute statistics"],
      c: 1,
      why: "Constraints are facts about the data, and facts let the optimiser eliminate work. NOT NULL and UNIQUE similarly change how it costs joins and null handling." },

    { q: "Which is the better long-term answer for “delete the customer” in a business system?",
      a: ["ON DELETE CASCADE through every related table",
          "A soft delete — a deleted_at column plus filtered indexes",
          "TRUNCATE the related tables",
          "Disable foreign keys and delete"],
      c: 1,
      why: "“Delete the customer” almost never means “destroy seven years of invoices”, and a tax authority may have opinions. Soft delete keeps the history and the referential integrity." },

    { q: "What does a DEFAULT clause guarantee?",
      a: ["That the column can never be null",
          "That a value is supplied when the INSERT does not name the column — nothing more",
          "That the value is validated",
          "That existing rows are updated"],
      c: 1,
      why: "An INSERT that explicitly writes NULL into the column still writes NULL. DEFAULT and NOT NULL are two separate decisions and you usually want both." },

    { q: "Which constraint enforces “an order that is shipped must have a ship date not earlier than its order date”?",
      a: ["FOREIGN KEY", "UNIQUE", "CHECK", "DEFAULT"],
      c: 2,
      why: "A CHECK can reference several columns of the same row. Multi-row or cross-table rules are the ones a CHECK cannot express — those need a trigger or an assertion, and are much more expensive." },

    { q: "You add a UNIQUE constraint on <code>email</code> and it fails. What is the sensible order of operations?",
      a: ["Drop the constraint and enforce it in application code",
          "Find and resolve the existing duplicates, then add the constraint",
          "Add it WITH NOCHECK",
          "Delete all rows with duplicate emails immediately"],
      c: 1,
      why: "The constraint failing is the constraint working: it is telling you the data already violates the rule you believed. Resolving deliberately — which row wins? merge or delete? — is part of the job." },

    { q: "A naming convention like fk_orders_customers, uq_customers_vat, ck_orders_total is worth adopting mainly because:",
      a: ["It is required by most engines",
          "Error messages, migration scripts and code review all become self-explanatory",
          "It improves performance",
          "It allows longer names"],
      c: 1,
      why: "Constraint names are read by humans in exactly the moments when reading time is expensive: a failed deploy, a production error, a code review at the end of a sprint." }
  ],

  /* ------------------------------------------------- 05 · normalisation -- */
  "05-normalisation": [
    { q: "What problem does normalisation solve?",
      a: ["Slow queries",
          "Redundancy, and therefore the update, insertion and deletion anomalies it makes possible",
          "Disk space only",
          "Concurrency"],
      c: 1,
      why: "A fact stored twice can be changed in one place and not the other. Every normal form removes a class of that." },

    { q: "A table has the composite key (order_id, sku) and also a column product_name. Which normal form does that violate?",
      a: ["1NF", "2NF", "3NF", "BCNF"],
      c: 1,
      why: "product_name depends on sku alone — part of the key, not the whole key. That is a partial dependency, which is exactly what 2NF forbids. 2NF is only ever relevant when the key is composite." },

    { q: "An orders table contains customer_city and city_region. Which normal form does that violate?",
      a: ["1NF", "2NF", "3NF", "None"],
      c: 2,
      why: "city_region depends on customer_city, which is a non-key column — a transitive dependency. 3NF requires non-key columns to depend on the key and nothing else." },

    { q: "A column holds the value <code>\"A12 x2, B7 x1\"</code>. Which normal form is violated?",
      a: ["1NF", "2NF", "3NF", "BCNF"],
      c: 0,
      why: "1NF requires atomic values. A list in a column cannot be joined, filtered or aggregated without string parsing, which is neither indexable nor reliable." },

    { q: "What is the practical target for a transactional (OLTP) schema?",
      a: ["1NF", "3NF", "5NF", "Fully denormalised"],
      c: 1,
      why: "3NF removes essentially all the anomalies that occur in practice. BCNF and beyond matter in narrow cases; naming them shows you read the book, knowing 3NF is where the value is shows you have shipped one." },

    { q: "Which of these is a legitimate reason to denormalise?",
      a: ["It feels faster",
          "A measured read problem the normalised design cannot solve, an analytical workload, or a point-in-time capture",
          "To reduce the number of tables",
          "Because joins are always slow"],
      c: 1,
      why: "The word 'measured' is load-bearing. Denormalising before you have a slow query is how you get a fast wrong answer that is expensive to fix." },

    { q: "When you denormalise, what must you also state?",
      a: ["The disk saving",
          "The mechanism keeping the copy correct and the staleness that is acceptable",
          "Which index to add",
          "The normal form you left"],
      c: 1,
      why: "A denormalised column with no named refresh mechanism is a bug that has not happened yet. Trigger, scheduled refresh, materialised view, or an explicit tolerance — pick one and say it." },

    { q: "Which mnemonic captures 1NF through 3NF?",
      a: ["Atomic, unique, indexed",
          "The key, the whole key, and nothing but the key",
          "One table per entity",
          "Normalise until it hurts"],
      c: 1,
      why: "Correct — but say it and then give an example of each violation, because the mnemonic on its own reads as memorised rather than understood." },

    { q: "A star schema is deliberately denormalised. Why is that not a contradiction?",
      a: ["Analytical data is less important",
          "Its workload is different: bulk-loaded, rarely updated, read with wide aggregate scans — so update anomalies largely cannot arise",
          "Normal forms only apply to small tables",
          "It is a contradiction, and star schemas are a mistake"],
      c: 1,
      why: "Normalisation protects against anomalies from concurrent piecemeal updates. A dimension table rebuilt by a controlled load has a different risk profile, and wide scans benefit from fewer joins." },

    { q: "Which anomaly does this describe: “we cannot record a new supplier until they have at least one product, because supplier details only exist on product rows”?",
      a: ["Update anomaly", "Insertion anomaly", "Deletion anomaly", "Referential anomaly"],
      c: 1,
      why: "Insertion anomaly. Its mirror image is the deletion anomaly: removing the supplier's last product also erases their address." }
  ],

  /* -------------------------------------- 06 · how a SELECT is evaluated -- */
  "06-how-select-is-evaluated": [
    { q: "What is the logical evaluation order of a SELECT statement?",
      a: ["SELECT, FROM, WHERE, GROUP BY, HAVING, ORDER BY",
          "FROM, WHERE, GROUP BY, HAVING, SELECT, DISTINCT, ORDER BY, LIMIT",
          "FROM, SELECT, WHERE, GROUP BY, ORDER BY",
          "WHERE, FROM, SELECT, GROUP BY, ORDER BY"],
      c: 1,
      why: "The two clauses you write first are evaluated fifth and first. Nearly every beginner error in SQL is this one fact." },

    { q: "Why can ORDER BY use a SELECT alias when WHERE cannot?",
      a: ["ORDER BY is more permissive by convention",
          "Because ORDER BY is evaluated after SELECT, where the alias is created, and WHERE is evaluated before it",
          "Because WHERE runs on the server and ORDER BY on the client",
          "It is a bug in the standard"],
      c: 1,
      why: "The asymmetry looks arbitrary until you know the order, and then it is obvious — which is exactly why it is asked." },

    { q: "A condition could be written in either WHERE or HAVING. Where should it go, and why?",
      a: ["HAVING, because it is evaluated later",
          "WHERE, because it can use an index and it reduces the rows the aggregate has to process",
          "Either — there is no difference",
          "HAVING, because it is clearer"],
      c: 1,
      why: "Correct either way; only one of them avoids the work. Saying why it is faster is the half of the answer that lands." },

    { q: "A LEFT JOIN is returning fewer rows than the left table has. What is the most likely cause?",
      a: ["The join key is null",
          "A predicate on the right-hand table is in WHERE instead of ON, so the null-padded rows are filtered out",
          "The right table is empty",
          "DISTINCT was used"],
      c: 1,
      why: "The join produces nulls for unmatched rows; a WHERE predicate on those columns is UNKNOWN and the rows disappear. The outer join has been silently converted to an inner join." },

    { q: "Where should a condition go if it is part of the <em>matching rule</em> of an outer join?",
      a: ["WHERE", "ON", "HAVING", "It does not matter"],
      c: 1,
      why: "ON decides which rows pair up while still preserving unmatched left rows. WHERE filters the result after the join has already happened." },

    { q: "SELECT DISTINCT is not removing the duplicates you expected. What is the most likely reason?",
      a: ["DISTINCT does not work with joins",
          "A column that is unique per row — such as an id — is in the select list, so every projected row is already distinct",
          "The engine ignores DISTINCT with ORDER BY",
          "DISTINCT only applies to the first column"],
      c: 1,
      why: "DISTINCT applies to the whole projected row. And a DISTINCT added to fix duplicates usually means a fanning join, which DISTINCT hides rather than fixes." },

    { q: "The logical processing order describes what, exactly?",
      a: ["The exact sequence of operations the engine performs",
          "What the query MEANS — the optimiser may do anything that produces the same result",
          "The order the parser reads the text",
          "The order rows are returned"],
      c: 1,
      why: "The optimiser pushes predicates into joins, uses indexes to avoid sorts, and stops early for LIMIT. The order gives semantics; the execution plan gives behaviour. Saying both halves answers the follow-up before it is asked." },

    { q: "Which clause is evaluated last?",
      a: ["ORDER BY", "LIMIT / OFFSET / FETCH", "SELECT", "DISTINCT"],
      c: 1,
      why: "Which is why LIMIT without ORDER BY returns an arbitrary subset — there is no defined order for it to take the first n of." },

    { q: "Why does <code>WHERE spend &gt; 100000</code> fail when <code>spend</code> is defined as <code>SUM(total_cents) AS spend</code>?",
      a: ["SUM cannot be compared to a number",
          "The alias does not exist when WHERE runs, and aggregates are not available until after grouping",
          "spend is a reserved word",
          "It needs to be in quotes"],
      c: 1,
      why: "Two problems at once: the alias is created in SELECT (step 5) and the aggregate is computed in GROUP BY (step 3). Use HAVING SUM(...), or wrap the query." },

    { q: "Window functions are evaluated at which point?",
      a: ["Before WHERE",
          "After GROUP BY and HAVING, and before ORDER BY",
          "At the same time as aggregates",
          "Last, after LIMIT"],
      c: 1,
      why: "Which is precisely why you cannot filter on a window function in WHERE and every top-N-per-group query needs a wrapping CTE or derived table." }
  ],

  /* ------------------------------- 07 · predicates and sargability -------- */
  "07-predicates-and-sargability": [
    { q: "What does it mean for a predicate to be <em>sargable</em>?",
      a: ["That it returns few rows",
          "That the engine can use it to seek into an index rather than scanning",
          "That it uses only equality",
          "That it is written in standard SQL"],
      c: 1,
      why: "Search ARGument able. The requirement is that the indexed column appears alone and untouched on one side of the operator." },

    { q: "Which rewrite makes <code>WHERE YEAR(placed_at) = 2026</code> able to use an index?",
      code: "-- rewrite this:\nWHERE YEAR(placed_at) = 2026",
      a: ["WHERE placed_at LIKE '2026%'",
          "WHERE placed_at >= DATE '2026-01-01' AND placed_at < DATE '2027-01-01'",
          "WHERE CAST(placed_at AS DATE) BETWEEN '2026-01-01' AND '2026-12-31'",
          "WHERE placed_at IN (SELECT d FROM calendar WHERE year = 2026)"],
      c: 1,
      why: "A half-open range over the raw column. The index stores placed_at, not YEAR(placed_at), so any wrapping function makes it unusable." },

    { q: "Why is <code>BETWEEN '2026-01-01' AND '2026-12-31'</code> wrong for a timestamp column?",
      a: ["BETWEEN is not standard SQL",
          "It excludes everything on 31 December after midnight, because 23:59 is greater than that date at 00:00",
          "BETWEEN cannot be used with dates",
          "It includes an extra day"],
      c: 1,
      why: "The half-open form >= start AND < next_start is correct at any precision and stays correct if the column type changes later." },

    { q: "The column <code>vat_number</code> is VARCHAR and a query filters <code>WHERE vat_number = 12345678901</code>. What happens?",
      a: ["A syntax error",
          "The column is implicitly converted to a number on every row, so the index cannot be used — and the query still returns the right answer",
          "The literal is converted to a string and the index is used",
          "The query fails at runtime"],
      c: 1,
      why: "The hardest of the four sargability killers to spot, because nothing looks broken. Look for a convert warning in the plan whenever a query is slow and the index 'obviously' exists." },

    { q: "Which LIKE pattern can use a B-tree index?",
      a: ["'%rossi%'", "'%rossi'", "'rossi%'", "None of them"],
      c: 2,
      why: "An index is sorted by prefix, so a known prefix is a range. A leading wildcard has no prefix to seek on — that needs full-text search or a trigram index, not a B-tree." },

    { q: "A query filters <code>WHERE email = ? OR phone = ?</code> and scans despite indexes on both columns. What is the usual fix?",
      a: ["Add a composite index on (email, phone)",
          "Rewrite as UNION of two single-column queries",
          "Add OPTION (FORCESEEK)",
          "Nothing can be done"],
      c: 1,
      why: "No single index has both as a leading key. Splitting into two queries lets the optimiser cost and seek each half independently, then combine the results." },

    { q: "What is wrong with <code>WHERE (@customer_id IS NULL OR customer_id = @customer_id)</code>?",
      a: ["It is a syntax error",
          "One cached plan is reused for every combination of supplied and omitted parameters, so a plan built for one shape is used for all of them",
          "It always scans, by definition",
          "It returns wrong results when the parameter is null"],
      c: 1,
      why: "The optional-parameter pattern. Fixes are OPTION (RECOMPILE) or building the statement with safe, parameterised dynamic SQL. Naming it signals you have maintained a reporting screen." },

    { q: "You cannot change a vendor query that filters on <code>DATE_TRUNC('month', placed_at)</code>. What can you do?",
      a: ["Nothing",
          "Create an index on the expression itself, or a persisted computed column with an index on it",
          "Increase the buffer pool",
          "Add a hint"],
      c: 1,
      why: "An expression index (PostgreSQL, Oracle) or a PERSISTED computed column plus an index (SQL Server) makes the original predicate sargable against the new structure." },

    { q: "Which of these predicates is sargable?",
      a: ["WHERE total_cents / 100 > 500",
          "WHERE total_cents > 50000",
          "WHERE ABS(total_cents) > 50000",
          "WHERE CAST(total_cents AS VARCHAR) LIKE '5%'"],
      c: 1,
      why: "Move the arithmetic to the constant side. The column must appear alone." },

    { q: "An ORM binds a parameter as NVARCHAR against a VARCHAR column in SQL Server. What is the result?",
      a: ["An error",
          "An implicit conversion of the column, losing the index seek — a classic and hard-to-spot performance incident",
          "The parameter is converted instead, with no cost",
          "The query returns no rows"],
      c: 1,
      why: "Type precedence converts VARCHAR to NVARCHAR, which means converting the column, not the parameter. Fixing it usually means telling the ORM the correct column type." }
  ],

  /* ------------------------------------------------------- 08 · joins ---- */
  "08-joins": [
    { q: "Logically, what is <code>A JOIN B ON p</code>?",
      a: ["A lookup of B for each row of A",
          "The Cartesian product of A and B restricted to rows satisfying p",
          "A merge of two sorted lists",
          "A union of A and B"],
      c: 1,
      why: "Outer joins then add back the unmatched rows from the preserved side, null-padded. That definition explains both the row counts and the null behaviour in the result." },

    { q: "Which query correctly finds customers who have never placed an order?",
      a: ["SELECT c.* FROM customers c LEFT JOIN orders o ON o.customer_id = c.id",
          "SELECT c.* FROM customers c WHERE c.id NOT IN (SELECT customer_id FROM orders)",
          "SELECT c.* FROM customers c WHERE NOT EXISTS (SELECT 1 FROM orders o WHERE o.customer_id = c.id)",
          "SELECT c.* FROM customers c JOIN orders o ON o.customer_id = c.id WHERE o.id IS NULL"],
      c: 2,
      why: "NOT EXISTS is null-safe. The NOT IN version returns zero rows if customer_id contains a single NULL, silently. The last option is an inner join, so it can never return anything." },

    { q: "Why does <code>x NOT IN (1, 2, NULL)</code> never return true?",
      a: ["NOT IN cannot contain NULL",
          "It expands to x <> 1 AND x <> 2 AND x <> NULL, and the last term is UNKNOWN, so the conjunction can never be TRUE",
          "It is a syntax error",
          "NULL is treated as zero"],
      c: 1,
      why: "Three-valued logic. The result is an empty set rather than an error, which is why this bug survives code review and appears months later as 'the report stopped showing anything'." },

    { q: "An order has 2 lines and 3 payments. Joining orders to both children in one query produces how many rows?",
      a: ["1", "3", "5", "6"],
      c: 3,
      why: "2 × 3 = 6. Two one-to-many joins from the same parent multiply, they do not add — so SUM(line_total) is 3× too big and SUM(payment) is 2× too big." },

    { q: "The totals on a report are exactly three times too high. What is the correct fix?",
      a: ["Add SELECT DISTINCT",
          "Divide by 3",
          "Pre-aggregate each child branch in a derived table or CTE, then join those one-to-one",
          "Use a LEFT JOIN instead"],
      c: 2,
      why: "DISTINCT removes duplicate rows but leaves the sums wrong, and hides the actual bug. Aggregating each branch first makes each side contribute exactly one row per parent." },

    { q: "What is a <em>semi-join</em>?",
      a: ["A join that returns half the columns",
          "Rows from the left table that have at least one match, without duplication and without right-hand columns — written as EXISTS",
          "A LEFT JOIN with a WHERE clause",
          "A join on a partial key"],
      c: 1,
      why: "It is the difference between 'customers who have ordered' (one row each) and 'customers joined to their orders' (one row per order)." },

    { q: "In <code>SELECT c.name, COUNT(*) FROM customers c LEFT JOIN orders o ON …</code>, what does COUNT(*) report for a customer with no orders?",
      a: ["0", "1", "NULL", "An error"],
      c: 1,
      why: "The null-padded row is still a row. Use COUNT(o.id), which counts non-null values and therefore correctly reports 0." },

    { q: "A nested loops join whose outer input was estimated at 10 rows actually produced 400 000. What has happened?",
      a: ["The join type is wrong for the query",
          "The optimiser chose loops because it expected a small outer input; the estimate was wrong, so it is now performing 400 000 index seeks",
          "The index is fragmented",
          "The query needs a hint"],
      c: 1,
      why: "The classic 'it used to be fast' shape. The fix is upstream — statistics, or a predicate the optimiser cannot estimate — not the join operator itself." },

    { q: "Which join algorithm is normally chosen for two large unsorted inputs joined on equality?",
      a: ["Nested loops", "Hash join", "Merge join", "Cross join"],
      c: 1,
      why: "It builds a hash table on the smaller input and probes with the larger. It needs memory, and if the estimate was too low it spills to disk — a common cause of a suddenly slow query." },

    { q: "When is a merge join the cheapest option?",
      a: ["When one input is tiny",
          "When both inputs are already sorted on the join key",
          "When the join is an inequality",
          "When there is no index"],
      c: 1,
      why: "It walks both inputs once. If they are not already sorted the optimiser has to add a sort, which usually makes hash cheaper." },

    { q: "What does a FULL OUTER JOIN give you that a LEFT JOIN does not?",
      a: ["Faster execution",
          "Rows from the right side that had no match on the left, as well as the reverse",
          "Deduplicated results",
          "Nothing — they are equivalent"],
      c: 1,
      why: "Which makes it the natural reconciliation tool: what exists in system A and not B, and in B and not A, in one pass." }
  ],

  /* -------------------------------- 09 · NULL and three-valued logic ----- */
  "09-null-and-three-valued-logic": [
    { q: "What does <code>NULL</code> represent?",
      a: ["Zero", "An empty string", "An unknown or inapplicable value", "A deleted value"],
      c: 2,
      why: "Not a value but the absence of one, which is why every comparison with it yields UNKNOWN rather than TRUE or FALSE." },

    { q: "In SQL's three-valued logic, what is <code>FALSE AND UNKNOWN</code>?",
      a: ["UNKNOWN", "FALSE", "TRUE", "An error"],
      c: 1,
      why: "Whatever the unknown turns out to be, the conjunction is false. Similarly TRUE OR UNKNOWN is TRUE. Three-valued logic is not arbitrary — it is 'is this true for every possible value of the unknown?'" },

    { q: "A column <code>amount</code> holds 10, 20, NULL and 30. What does <code>AVG(amount)</code> return?",
      a: ["15", "20", "NULL", "60"],
      c: 1,
      why: "60 / 3, not 60 / 4 — AVG divides by the count of non-null values. If a missing amount means zero rather than unknown, that average is wrong and you need AVG(COALESCE(amount, 0))." },

    { q: "A CHECK constraint's expression evaluates to UNKNOWN for a row. Is the row accepted?",
      a: ["No — only TRUE is accepted",
          "Yes — CHECK rejects only rows where the expression is FALSE",
          "It depends on the engine's strict mode",
          "The insert errors"],
      c: 1,
      why: "The reverse of WHERE, which keeps only TRUE. This is why CHECK (discount < 100) does not stop a NULL discount, and why NOT NULL is a separate constraint." },

    { q: "How do PostgreSQL and SQL Server differ on NULL ordering by default?",
      a: ["They are the same",
          "PostgreSQL sorts nulls last ascending; SQL Server sorts them first",
          "PostgreSQL sorts nulls first; SQL Server sorts them last",
          "Neither defines an order"],
      c: 1,
      why: "Oracle matches PostgreSQL, MySQL and SQLite match SQL Server. If the position matters, say so with NULLS FIRST/LAST or an explicit CASE in the ORDER BY." },

    { q: "You need “at most one active cart per customer”, where inactive rows are unbounded. What enforces it best?",
      a: ["A UNIQUE constraint on (customer_id)",
          "A UNIQUE constraint on (customer_id, status)",
          "A filtered/partial unique index on (customer_id) WHERE status = 'active'",
          "A CHECK constraint"],
      c: 2,
      why: "Relying on null semantics for this is fragile and differs between engines. A partial unique index states the rule exactly, in one line, and is enforced by the engine." },

    { q: "What does <code>NULLIF(total, 0)</code> achieve in <code>delivered / NULLIF(total, 0)</code>?",
      a: ["It converts zero to one",
          "It turns a divide-by-zero into NULL, which is honest for 'undefined', with no error raised",
          "It rounds the result",
          "It filters out zero rows"],
      c: 1,
      why: "The idiomatic division guard. Whether to then display the null as 0 or as a dash is a separate presentation decision — COALESCE around the outside." },

    { q: "What is the result of <code>'Rossi' || NULL</code> in PostgreSQL?",
      a: ["'Rossi'", "NULL", "An empty string", "An error"],
      c: 1,
      why: "Concatenation propagates null. CONCAT() is the function that deliberately ignores nulls instead — a useful distinction when building display names." },

    { q: "Which comparison is null-safe in PostgreSQL and SQLite?",
      a: ["=", "IS NOT DISTINCT FROM", "<>", "COALESCE(a, b) = COALESCE(c, d)"],
      c: 1,
      why: "MySQL spells it <=>. SQL Server has no equivalent, so you fall back to explicit IS NULL handling or COALESCE to a sentinel value that cannot occur." },

    { q: "Which is the best design question to ask about every nullable column?",
      a: ["Is it indexed?",
          "Does the null mean 'not yet known', 'not applicable', or 'nobody filled it in' — and is that a state the model should have?",
          "How large is it?",
          "Is it a foreign key?"],
      c: 1,
      why: "'Not yet known' is legitimate. 'Not applicable' often means the table is really two kinds of thing. 'Nobody filled it in' is a missing NOT NULL and a missing default." },

    { q: "<code>COUNT(*)</code> and <code>COUNT(column)</code> differ how?",
      a: ["They are identical",
          "COUNT(*) counts rows; COUNT(column) counts rows where that column is not null",
          "COUNT(*) is slower",
          "COUNT(column) counts distinct values"],
      c: 1,
      why: "In a LEFT JOIN that difference is the entire point: COUNT(*) reports 1 for an unmatched row, COUNT(right.col) reports 0." }
  ],

  /* --------------------------------------------------- 10 · aggregation -- */
  "10-aggregation": [
    { q: "What does GROUP BY change about the result?",
      a: ["Only the order of rows",
          "The grain: one output row now represents a set of input rows",
          "The data types",
          "Nothing, without an aggregate"],
      c: 1,
      why: "Which is why every selected column must be a grouping column or inside an aggregate — for anything else, 'which of the twelve values do you mean?' has no answer." },

    { q: "Is <code>COUNT(1)</code> faster than <code>COUNT(*)</code>?",
      a: ["Yes, significantly", "Yes, marginally", "No — every mainstream optimiser treats them identically", "It depends on the index"],
      c: 2,
      why: "Folklore. The real distinction is COUNT(column), which skips nulls and answers a different question." },

    { q: "A dashboard with four <code>COUNT(DISTINCT …)</code> expressions is slow. Why is that the first thing to look at?",
      a: ["DISTINCT is not supported in aggregates",
          "Each requires a sort or hash of all values and they parallelise poorly, so several in one query multiply the cost",
          "They force a table scan",
          "They break the plan cache"],
      c: 1,
      why: "Fixes are pre-aggregation into a summary table, or an approximate distinct count if the number is a dashboard tile rather than an invoice line." },

    { q: "Which condition belongs in HAVING rather than WHERE?",
      a: ["status <> 'cancelled'",
          "placed_at >= DATE '2026-01-01'",
          "SUM(total_cents) > 100000",
          "customer_id = 42"],
      c: 2,
      why: "Only a condition on an aggregate needs HAVING. Everything else belongs in WHERE, where it can use an index and reduce what the aggregate has to read." },

    { q: "What does <code>COUNT(CASE WHEN status = 'shipped' THEN 1 END)</code> count?",
      a: ["All rows",
          "Rows where status is 'shipped' — because non-matches yield NULL and COUNT ignores nulls",
          "Distinct statuses",
          "Nothing without an ELSE"],
      c: 1,
      why: "Conditional aggregation. Omitting the ELSE is deliberate: adding ELSE 0 would make COUNT count everything. For SUM the opposite holds — ELSE 0 is harmless and clearer." },

    { q: "What advantage does conditional aggregation have over running several separate queries?",
      a: ["Shorter code only",
          "One pass over the data instead of one per question",
          "It avoids GROUP BY",
          "It is more portable"],
      c: 1,
      why: "Three status counts become one scan rather than three. On a large table that is the difference between a report that opens and one that times out." },

    { q: "What does <code>GROUP BY ROLLUP (region, city)</code> produce?",
      a: ["Only per-city totals",
          "Per region+city rows, per-region subtotals, and a grand total, in one pass",
          "A pivoted result",
          "All combinations of both columns"],
      c: 1,
      why: "CUBE gives all combinations; GROUPING SETS gives exactly the ones you list. The subtotal rows carry NULLs, and GROUPING() tells you which nulls the rollup created." },

    { q: "Standard SQL rejects selecting a non-grouped, non-aggregated column. What did MySQL historically do?",
      a: ["The same",
          "Returned an arbitrary value from the group, which is worse than an error because it looks like it worked",
          "Returned NULL",
          "Returned the first row inserted"],
      c: 1,
      why: "ONLY_FULL_GROUP_BY is now on by default. Both MySQL and PostgreSQL do allow selecting columns functionally dependent on the grouping key — legal, convenient and not portable to SQL Server." },

    { q: "In a report of monthly revenue, why filter <code>status &lt;&gt; 'cancelled'</code> in WHERE rather than HAVING?",
      a: ["HAVING would be a syntax error",
          "It is not an aggregate condition, so WHERE lets it use an index and removes rows before the aggregate reads them",
          "HAVING cannot reference status",
          "There is no difference"],
      c: 1,
      why: "Same answer, less work. Putting non-aggregate conditions in HAVING means grouping rows you are about to discard." },

    { q: "How many rows does <code>SELECT COUNT(*) FROM orders</code> return when orders is empty?",
      a: ["Zero rows", "One row containing 0", "One row containing NULL", "An error"],
      c: 1,
      why: "An aggregate with no GROUP BY always returns exactly one row. Note that SUM over no rows returns NULL, not 0 — a common source of nulls appearing in a report where you expected a zero." }
  ],

  /* ---------------------------------------------------- 11 · subqueries -- */
  "11-subqueries": [
    { q: "Which is the null-safe way to express “rows with no matching child”?",
      a: ["NOT IN (SELECT …)", "NOT EXISTS (SELECT 1 …)", "<> ALL (SELECT …)", "LEFT JOIN with a WHERE on a non-null column"],
      c: 1,
      why: "NOT EXISTS handles nulls correctly and usually plans the same or better. NOT IN returns the empty set if the subquery yields a single null." },

    { q: "Is “EXISTS is faster than IN” true?",
      a: ["Yes, always",
          "No — modern optimisers rewrite both into the same internal semi-join and usually produce identical plans",
          "Yes, in SQL Server only",
          "Only for correlated subqueries"],
      c: 1,
      why: "Choose on semantics, not folklore. The real, non-negotiable difference is the negative form and nulls." },

    { q: "What distinguishes a <em>correlated</em> subquery?",
      a: ["It returns more than one row",
          "It references a column from the outer query, so it is conceptually evaluated per outer row",
          "It appears in the FROM clause",
          "It uses EXISTS"],
      c: 1,
      why: "A good optimiser often decorrelates it into a join. When it cannot — a scalar UDF inside, a TOP with a tie-break — you have written an N+1 inside the database." },

    { q: "A query has four correlated subqueries over the same child table. What is the standard rewrite?",
      a: ["Add indexes on the child table",
          "Combine them into one pre-aggregated derived table joined once",
          "Convert them to EXISTS",
          "Move them into the SELECT list"],
      c: 1,
      why: "One GROUP BY plus one join is a single pass instead of four executions per outer row. It is a favourite 'optimise this' exercise because the fix is mechanical once you see it." },

    { q: "What does <code>LATERAL</code> (or <code>CROSS APPLY</code>) allow that a plain derived table does not?",
      a: ["Returning multiple columns",
          "Referencing columns of tables to its left in the FROM clause",
          "Using aggregates",
          "Recursion"],
      c: 1,
      why: "Which makes top-N-per-group directly expressible, and often faster than ranking, because it can stop after n rows per group rather than ranking all of them." },

    { q: "What is the difference between CROSS APPLY and OUTER APPLY?",
      a: ["CROSS APPLY is faster",
          "CROSS APPLY drops left rows whose subquery returned nothing; OUTER APPLY keeps them with nulls",
          "OUTER APPLY allows aggregates",
          "They are synonyms"],
      c: 1,
      why: "Exactly the relationship between an inner and a left join — and exactly the same bug when you pick the wrong one." },

    { q: "A scalar subquery in a SELECT list returns two rows at runtime. What happens?",
      a: ["The first row is used", "NULL is returned", "A runtime error", "The rows are concatenated"],
      c: 2,
      why: "A scalar subquery must return at most one row. Returning zero rows gives NULL; returning more than one is an error, and it is a runtime error, so it can appear months after deployment when the data changes." },

    { q: "Which is the correct portable way to get the three most recent orders per customer?",
      a: ["ORDER BY placed_at DESC LIMIT 3",
          "ROW_NUMBER() OVER (PARTITION BY customer_id ORDER BY placed_at DESC) filtered to <= 3 in an outer query",
          "GROUP BY customer_id with MAX(placed_at)",
          "DISTINCT ON (customer_id)"],
      c: 1,
      why: "The window-function form is portable. LATERAL/APPLY is often faster with the right index, and DISTINCT ON is PostgreSQL-only and gives just one row." },

    { q: "Why must a derived table in FROM be given an alias?",
      a: ["For readability only",
          "Because the standard requires the result to be nameable so its columns can be referenced",
          "To allow indexing",
          "It does not have to be"],
      c: 1,
      why: "PostgreSQL and MySQL enforce it strictly; SQL Server and Oracle are more forgiving in places. Aliasing everything is a good habit for portability regardless." },

    { q: "<code>WHERE x &gt; ALL (SELECT y FROM t)</code> where t is empty returns what?",
      a: ["No rows", "All rows", "An error", "NULL"],
      c: 1,
      why: "Vacuous truth: x is greater than every element of an empty set. The mirror case, > ANY over an empty set, is false for every row. Both surprise people and both come from set logic, not from SQL quirks." }
  ],

  /* ------------------------------------------- 12 · CTEs and recursion --- */
  "12-ctes-and-recursion": [
    { q: "In SQL Server and MySQL 8, an expensive CTE referenced three times is evaluated how many times?",
      a: ["Once", "Three times", "It depends on statistics", "Once, then cached"],
      c: 1,
      why: "The CTE is inlined into the outer query. 'I put it in a CTE so it only runs once' is simply false there. For guaranteed single evaluation use a temp table — which also gets statistics — or PostgreSQL's AS MATERIALIZED." },

    { q: "What did PostgreSQL change about CTEs in version 12?",
      a: ["They became recursive",
          "They are now inlined when referenced once and side-effect free, instead of always materialised",
          "They were removed",
          "They became indexable"],
      c: 1,
      why: "Before 12, a CTE was an unconditional optimisation fence and was used deliberately to stop predicate push-down. AS MATERIALIZED and AS NOT MATERIALIZED now let you choose explicitly." },

    { q: "What are the two parts of a recursive CTE?",
      a: ["A SELECT and an INSERT",
          "An anchor member and a recursive member, combined with UNION ALL",
          "A base table and an index",
          "A CTE and a subquery"],
      c: 1,
      why: "The anchor runs once and produces the first working set; the recursive member joins that working set — not the accumulated result — to produce the next." },

    { q: "Why does a recursive CTE terminate?",
      a: ["A built-in depth limit",
          "Because each iteration sees only the PREVIOUS iteration's rows, so when an iteration produces none, it stops",
          "Because UNION removes duplicates",
          "It does not — it must be limited manually"],
      c: 1,
      why: "That is the mechanism. But real hierarchy data contains cycles, so you still need a depth guard — an employee who is their own grandparent will loop until the server intervenes." },

    { q: "What is the practical difference between UNION and UNION ALL in a recursive CTE?",
      a: ["None",
          "UNION deduplicates each iteration, which also breaks cycles, at the cost of a sort per step",
          "UNION ALL is not permitted",
          "UNION is faster"],
      c: 1,
      why: "UNION ALL is the normal choice. UNION's deduplication is occasionally a useful cycle-breaker in messy graph data, but you pay for it every iteration." },

    { q: "SQL Server's default recursion limit is 100. How do you raise or remove it?",
      a: ["It cannot be changed",
          "OPTION (MAXRECURSION n), with 0 meaning unlimited",
          "SET RECURSION_LIMIT",
          "A trace flag"],
      c: 1,
      why: "MAXRECURSION 0 removes the safety net entirely, so pair it with an explicit depth column and a WHERE bound. The default exists for good reason." },

    { q: "For a hierarchy that is read constantly and changed rarely, what is a good alternative to recursion?",
      a: ["Denormalise into a single row",
          "Store a materialised path, nested sets, or hierarchyid, so a subtree query becomes one indexed range scan",
          "Cache the query result",
          "Use a JSON column"],
      c: 1,
      why: "'/1/14/97/' with LIKE '/1/14/%' is a single indexed seek. It costs more on write and needs maintenance when a node moves — naming that trade-off is the senior answer." },

    { q: "Which restriction applies to the recursive member of a recursive CTE?",
      a: ["It must be a SELECT *",
          "It may reference the CTE only once, and not inside an outer join, aggregate or subquery",
          "It may not use WHERE",
          "It must join to the anchor"],
      c: 1,
      why: "The restriction is what makes the fixed-point evaluation well-defined and terminating." },

    { q: "What is a common use of a recursive CTE that has nothing to do with hierarchies?",
      a: ["Deduplication",
          "Generating a series — for example every date in a range, to fill gaps in a report",
          "Pivoting",
          "Index maintenance"],
      c: 1,
      why: "A date spine. PostgreSQL has generate_series and SQL Server 2022 has GENERATE_SERIES, but a recursive CTE works everywhere and needs no calendar table." },

    { q: "What is the main practical benefit of a CTE over nested derived tables?",
      a: ["Better performance",
          "Readability and debuggability: named steps read top to bottom and each one can be run alone",
          "It is materialised",
          "It allows more joins"],
      c: 1,
      why: "Performance is usually identical because it is inlined. The value is that the reader's eye starts at the top rather than in the middle of a nest." }
  ],

  /* --------------------------------------------- 13 · window functions --- */
  "13-window-functions": [
    { q: "How does a window function differ from an aggregate with GROUP BY?",
      a: ["It is faster",
          "It computes a value over related rows WITHOUT collapsing the result — the row count is unchanged",
          "It can only be used with ORDER BY",
          "It cannot use PARTITION BY"],
      c: 1,
      why: "GROUP BY puts receipts into envelopes and you lose the receipts. A window function writes a note in the corner of each one." },

    { q: "Salaries are 5000, 5000, 4000, 3000. What does RANK() return for the 4000?",
      a: ["2", "3", "4", "1"],
      c: 1,
      why: "RANK skips after ties: 1, 1, 3, 4. DENSE_RANK gives 1, 1, 2, 3. ROW_NUMBER gives 1, 2, 3, 4." },

    { q: "Which function should find “the second-highest distinct salary”?",
      a: ["ROW_NUMBER()", "RANK()", "DENSE_RANK()", "NTILE(2)"],
      c: 2,
      why: "DENSE_RANK does not skip, so rank 2 really is the second distinct value. ROW_NUMBER would return the second row, which is the same salary again if the top two are tied." },

    { q: "Why does <code>WHERE ROW_NUMBER() OVER (…) = 1</code> fail?",
      a: ["ROW_NUMBER needs PARTITION BY",
          "Window functions are evaluated after WHERE, so the value does not exist yet — wrap the query in a CTE or derived table",
          "It needs an alias",
          "ROW_NUMBER cannot be compared"],
      c: 1,
      why: "The same reason a SELECT alias cannot be used in WHERE. Every top-N-per-group query therefore has two levels." },

    { q: "Why must ROW_NUMBER carry a deterministic tie-breaker when it decides which row to keep?",
      a: ["For readability",
          "Because ties are broken arbitrarily and the arbitrary choice can change when the plan changes, so a de-duplication job could delete a different row each run",
          "Because it is required syntax",
          "To make it sargable"],
      c: 1,
      why: "ORDER BY updated_at DESC, id DESC. Without the id, the same job produces different results on different days — a genuinely horrible bug to diagnose." },

    { q: "Which index removes the sort from <code>ROW_NUMBER() OVER (PARTITION BY customer_id ORDER BY placed_at)</code>?",
      a: ["An index on (placed_at)",
          "An index on (customer_id, placed_at)",
          "An index on (placed_at, customer_id)",
          "No index can help"],
      c: 1,
      why: "Partition columns first, then order columns. Without it the engine sorts, and a sort with a bad estimate spills to disk — the usual cause of a slow window query." },

    { q: "What does <code>SUM(x) OVER (PARTITION BY c)</code>, with no ORDER BY, compute?",
      a: ["A running total",
          "The total for the whole partition, repeated on every row of it",
          "The value of x",
          "An error"],
      c: 1,
      why: "No ORDER BY means the frame is the whole partition. Adding an ORDER BY changes the default frame and turns it into a running total — the most surprising behaviour in the feature." },

    { q: "Where can a window function NOT appear?",
      a: ["In the SELECT list", "In ORDER BY", "In WHERE, GROUP BY or HAVING", "In a CTE"],
      c: 2,
      why: "Because those clauses are all evaluated before window functions are computed. SELECT and ORDER BY come after, so both are fine." },

    { q: "What does the WINDOW clause do?",
      a: ["Limits the number of rows",
          "Names a window definition so several functions can reuse it without repeating it",
          "Sets the frame size",
          "Creates an index"],
      c: 1,
      why: "WINDOW w AS (PARTITION BY … ORDER BY …). Supported in PostgreSQL, MySQL 8, SQLite and Oracle; not in SQL Server. It removes the risk of a typo in one of five copies." },

    { q: "Which is the correct pattern for “keep the newest row per email, delete the rest”?",
      a: ["DELETE FROM t WHERE ROW_NUMBER() > 1",
          "Rank in a CTE with PARTITION BY email ORDER BY updated_at DESC, id DESC, then delete where rn > 1",
          "SELECT DISTINCT into a new table",
          "GROUP BY email HAVING COUNT(*) > 1"],
      c: 1,
      why: "And run the SELECT half first to check the count, inside a transaction. Then add the UNIQUE constraint that should have prevented the duplicates." },

    { q: "What does NTILE(4) do?",
      a: ["Returns every fourth row",
          "Distributes the ordered rows into four buckets as evenly as possible, labelling each row 1–4",
          "Returns the top 25%",
          "Computes quartile boundaries"],
      c: 1,
      why: "Note it distributes rows, not values — so equal values can land in different buckets. For true value-based quartiles use PERCENTILE_CONT." }
  ],

  /* --------------------------------------- 14 · frames, gaps and islands - */
  "14-frames-and-islands": [
    { q: "With <code>ORDER BY</code> present and no explicit frame, what is the default window frame?",
      a: ["ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW",
          "RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW",
          "The whole partition",
          "ROWS BETWEEN 1 PRECEDING AND 1 FOLLOWING"],
      c: 1,
      why: "RANGE, not ROWS. With duplicate ordering values that produces different — and usually unintended — results, and it is generally slower." },

    { q: "Three orders share the same date. Using the default frame, what does the running total show for each of them?",
      a: ["Increasing values as each row is added",
          "The same day-end total for all three, because RANGE includes all peer rows",
          "Only the first row's value",
          "NULL for the second and third"],
      c: 1,
      why: "This is the 'my running total jumps' bug. Write ROWS UNBOUNDED PRECEDING to get per-row accumulation." },

    { q: "Which frame gives a 7-row moving average ending at the current row?",
      a: ["ROWS BETWEEN 7 PRECEDING AND CURRENT ROW",
          "ROWS BETWEEN 6 PRECEDING AND CURRENT ROW",
          "RANGE BETWEEN 7 PRECEDING AND CURRENT ROW",
          "ROWS BETWEEN CURRENT ROW AND 6 FOLLOWING"],
      c: 1,
      why: "6 preceding plus the current row is 7 rows. The off-by-one here is a classic review comment." },

    { q: "When is RANGE clearly better than ROWS?",
      a: ["Never",
          "With an INTERVAL — 'the last 7 days' is not 'the last 7 rows' when some days have no data",
          "For ranking functions",
          "When the partition is large"],
      c: 1,
      why: "RANGE BETWEEN INTERVAL '7 days' PRECEDING AND CURRENT ROW. Supported by PostgreSQL and Oracle; SQL Server does not have it." },

    { q: "<code>LAST_VALUE(x) OVER (ORDER BY d)</code> returns what, and why?",
      a: ["The last value in the partition",
          "The current row's value, because the default frame ends at the current row",
          "NULL",
          "The first value"],
      c: 1,
      why: "You must extend the frame to UNBOUNDED FOLLOWING. In practice FIRST_VALUE over a reversed ORDER BY is easier to read and harder to get wrong." },

    { q: "What is the third argument to <code>LAG(amount, 1, 0)</code>?",
      a: ["The partition", "The offset", "The default value when there is no previous row", "The frame size"],
      c: 2,
      why: "Without it, the first row of each partition gets NULL, and any arithmetic on it propagates that null through your delta column." },

    { q: "Days present are 1, 2, 3, 7, 8, 12. Why does grouping by <code>day − ROW_NUMBER()</code> find the consecutive runs?",
      a: ["Because ROW_NUMBER is always consecutive",
          "Because within a run both increase by exactly 1, so their difference is constant",
          "Because it deduplicates",
          "Because it sorts the data"],
      c: 1,
      why: "The differences are 0, 0, 0, 3, 3, 6 — one constant per island. Group by that and take MIN and MAX of the day." },

    { q: "The gaps-and-islands subtraction trick assumes what?",
      a: ["That the values are unique",
          "That the step between consecutive values is fixed",
          "That there are no nulls",
          "That the table is indexed"],
      c: 1,
      why: "For irregular data — sessions with a 30-minute idle threshold — use the general form: flag where a new group starts, then take a running SUM of the flag as the group id." },

    { q: "In the sessionisation pattern, what does <code>SUM(is_new_session) OVER (… ROWS UNBOUNDED PRECEDING)</code> produce?",
      a: ["The number of sessions",
          "A session id: it increments exactly when a new session starts and stays constant within one",
          "The session duration",
          "A row number"],
      c: 1,
      why: "A running sum of a 0/1 flag is the general-purpose grouping key for any 'runs of consecutive things' problem." },

    { q: "Which is true of <code>SUM(x) OVER (ORDER BY d ROWS UNBOUNDED PRECEDING)</code>?",
      a: ["It is the partition total",
          "It is a running total that accumulates row by row, including with duplicate dates",
          "It requires PARTITION BY",
          "It is the same as SUM(x) OVER ()"],
      c: 1,
      why: "The explicit ROWS is what distinguishes it from the RANGE default, which would give every peer row the same value." }
  ],

  /* ----------------------------------------------- 15 · set operations --- */
  "15-set-operations": [
    { q: "What is the cost difference between UNION and UNION ALL?",
      a: ["None",
          "UNION must deduplicate the combined result — a sort or hash over every row — while UNION ALL just concatenates",
          "UNION ALL sorts, UNION does not",
          "UNION uses less memory"],
      c: 1,
      why: "On a large result that is often the difference between two seconds and forty. Default to UNION ALL and use UNION only when duplicates genuinely must go." },

    { q: "How do set operators treat two NULLs when comparing rows?",
      a: ["As different, like the = operator",
          "As equal — which is the opposite of = and is what makes EXCEPT a good table-diff tool",
          "They raise an error",
          "They are ignored"],
      c: 1,
      why: "So EXCEPT correctly reports 'no difference' when both sides have a null in the same column, which a join on = would not." },

    { q: "Where does ORDER BY go in a query using UNION?",
      a: ["In each branch",
          "Once, at the very end, applying to the whole result",
          "It is not permitted",
          "In the first branch only"],
      c: 1,
      why: "To order within a branch you need a derived table. Column names for the result come from the first branch." },

    { q: "Which pair of queries shows exactly what differs between two tables?",
      a: ["A FULL OUTER JOIN on the primary key",
          "A EXCEPT B, and B EXCEPT A",
          "A INTERSECT B",
          "SELECT DISTINCT from both"],
      c: 1,
      why: "It compares whole rows, needs no join keys, and treats nulls as equal — so it will not report a null-to-null match as a difference." },

    { q: "Which de-duplication approach is the most portable?",
      a: ["DELETE using ctid or ROWID",
          "ROW_NUMBER() OVER (PARTITION BY key ORDER BY …) with a delete of rn > 1",
          "SELECT DISTINCT into a new table",
          "GROUP BY with HAVING COUNT(*) > 1"],
      c: 1,
      why: "ctid is PostgreSQL, ROWID is Oracle, and SQL Server has neither. ROW_NUMBER works everywhere and lets you specify which row wins." },

    { q: "What should you do immediately after removing duplicate rows?",
      a: ["Rebuild the indexes",
          "Add the UNIQUE constraint that would have prevented them",
          "Run VACUUM",
          "Update statistics"],
      c: 1,
      why: "Otherwise you are doing it again next month. Fixing the symptom without the cause is what makes a data-cleanup ticket recur." },

    { q: "A query has DISTINCT and several joins. What does that usually indicate?",
      a: ["Good practice",
          "A fanning join producing duplicate rows, which DISTINCT hides while leaving every SUM and COUNT wrong",
          "That the tables lack primary keys",
          "That an index is missing"],
      c: 1,
      why: "If the intent is 'rows that have at least one match', that is a semi-join — EXISTS — which is both correct and cheaper." },

    { q: "What must be true of the branches of a UNION?",
      a: ["Identical column names",
          "The same number of columns, in the same order, with compatible types",
          "The same table",
          "The same number of rows"],
      c: 1,
      why: "Names are taken from the first branch, which is why an unlabelled second branch never causes an error but can produce a confusingly named result." },

    { q: "Oracle spells EXCEPT how?",
      a: ["DIFFERENCE", "MINUS", "SUBTRACT", "NOT UNION"],
      c: 1,
      why: "Oracle 21c added EXCEPT as an alias, but MINUS is what you will meet in existing PL/SQL code." },

    { q: "For a very large de-duplication, why might rebuilding the table beat deleting rows?",
      a: ["It is always safer",
          "Deleting tens of millions of rows is slow and leaves bloat; CREATE TABLE AS SELECT DISTINCT plus a rename is often far faster",
          "It preserves indexes automatically",
          "It avoids locking"],
      c: 1,
      why: "The catch, and the thing that bites people: the new table has none of the original indexes, constraints or grants. Script them first." }
  ],

  /* ------------------------------------------------- 16 · CASE and pivot -- */
  "16-case-and-pivot": [
    { q: "In a searched CASE, which branch wins when several conditions are true?",
      a: ["The most specific one", "The first one in written order", "The last one", "It is undefined"],
      c: 1,
      why: "Which is why ordering matters and there is no warning: put >= 20000 before >= 100000 and every large value is labelled medium, silently." },

    { q: "What does a CASE expression return when no branch matches and there is no ELSE?",
      a: ["Zero", "An empty string", "NULL", "An error"],
      c: 2,
      why: "Always write an ELSE, so an unhandled value is visible rather than a null nobody investigates." },

    { q: "Why can a simple CASE (<code>CASE status WHEN … END</code>) not match NULL?",
      a: ["It is a syntax restriction",
          "It compares with equality, and nothing equals NULL",
          "NULL is not a valid status",
          "It can, with a special keyword"],
      c: 1,
      why: "Use a searched CASE with WHEN status IS NULL instead." },

    { q: "What is the portable way to pivot rows into columns?",
      a: ["The PIVOT operator",
          "Conditional aggregation: one SUM(CASE WHEN … THEN … END) per output column",
          "CROSSTAB",
          "A recursive CTE"],
      c: 1,
      why: "PIVOT exists in SQL Server and Oracle only, handles one aggregate, and still needs the column list written out — so it solves none of the real problem." },

    { q: "Why can a pivot's column list not be built from the data at query time?",
      a: ["Performance",
          "Because SQL returns a fixed set of columns decided at parse time, so a dynamic column list needs dynamic SQL",
          "Because CASE cannot be dynamic",
          "It can, using PIVOT"],
      c: 1,
      why: "And the usual right answer is not to write the dynamic SQL: return long data and let Power BI, SSRS or Excel pivot it, keeping the query static, cacheable and safe." },

    { q: "What is the risk of <code>ISNULL(short_col, 'a long default')</code> in T-SQL?",
      a: ["It is slower than COALESCE",
          "The result takes the first argument's type, so the default can be silently truncated",
          "It cannot take a literal",
          "It returns NULL"],
      c: 1,
      why: "COALESCE uses type precedence across all arguments and is standard SQL. Use it." },

    { q: "How do you unpivot columns jan, feb, mar into rows, portably?",
      a: ["The UNPIVOT operator",
          "UNION ALL of one SELECT per source column",
          "A recursive CTE",
          "CROSS JOIN"],
      c: 1,
      why: "A lateral VALUES list is more compact and makes one pass over the table, but UNION ALL works everywhere." },

    { q: "In <code>SUM(CASE WHEN status = 'x' THEN amount ELSE 0 END)</code>, why is ELSE 0 fine here but wrong inside COUNT?",
      a: ["It is wrong in both",
          "SUM ignores nulls anyway so ELSE 0 is harmless and clearer; COUNT counts any non-null, so ELSE 0 would count every row",
          "COUNT does not accept CASE",
          "SUM requires an ELSE"],
      c: 1,
      why: "The asymmetry catches people out and produces a column of row counts where a filtered count was intended." },

    { q: "Which produces a safe percentage column?",
      a: ["100.0 * a / b",
          "100.0 * a / NULLIF(b, 0)",
          "CASE WHEN b = 0 THEN 0 ELSE 100 * a / b END with integer division",
          "100 * a / b"],
      c: 1,
      why: "NULLIF guards the division and the 100.0 forces floating-point or decimal arithmetic. Plain 100 * a / b with integers silently truncates to 0 or 1." },

    { q: "What is the most common backing shape for an SSRS or Power BI report?",
      a: ["A stored procedure with a cursor",
          "One grouped query using conditional aggregation, returning long or lightly pivoted data in a single scan",
          "Several queries joined in the report",
          "A recursive CTE"],
      c: 1,
      why: "One scan, no subqueries, and the reporting tool does the visual pivoting. It is also the shape that caches and parameterises well." }
  ],

  /* -------------------------------- 17 · strings, dates and time zones --- */
  "17-strings-dates-timezones": [
    { q: "Which filter for March 2026 can use an index on placed_at?",
      a: ["WHERE MONTH(placed_at) = 3 AND YEAR(placed_at) = 2026",
          "WHERE placed_at >= '2026-03-01' AND placed_at < '2026-04-01'",
          "WHERE CAST(placed_at AS DATE) BETWEEN '2026-03-01' AND '2026-03-31'",
          "WHERE FORMAT(placed_at, 'yyyy-MM') = '2026-03'"],
      c: 1,
      why: "Half-open, on the raw column. Every other option wraps the column and forces a scan — and the third also loses the last day." },

    { q: "Why is storing local time for an event a bug?",
      a: ["It uses more space",
          "One hour is repeated each autumn when the clocks go back, so rows recorded in it cannot be ordered or subtracted correctly, ever",
          "Local time cannot be indexed",
          "It is not a bug"],
      c: 1,
      why: "And it is unrecoverable: the information needed to disambiguate was never written down." },

    { q: "Should you store a time zone as an offset (+02:00) or a name (Europe/Rome)?",
      a: ["The offset — it is unambiguous",
          "The name — an offset is a snapshot of a rule that changes seasonally and historically",
          "Either",
          "Neither; store UTC only"],
      c: 1,
      why: "Rome is +01:00 in winter and +02:00 in summer, and DST rules have changed before. For a future appointment, the offset may simply be wrong by the time it arrives." },

    { q: "<code>DATEDIFF(day, '2026-03-14 23:59', '2026-03-15 00:01')</code> returns what?",
      a: ["0", "1", "2", "0.0014"],
      c: 1,
      why: "It counts day boundaries crossed, not elapsed time — so 'orders older than 30 days' is subtly wrong at the edges. If you mean elapsed time, subtract and compare to an interval." },

    { q: "What is the difference between T-SQL's LEN() and DATALENGTH()?",
      a: ["None",
          "LEN ignores trailing spaces and counts characters; DATALENGTH counts bytes, so NVARCHAR doubles",
          "DATALENGTH ignores leading spaces",
          "LEN counts bytes"],
      c: 1,
      why: "Validating an IBAN or VAT number with LEN() silently accepts trailing whitespace. Standard CHAR_LENGTH does not have this behaviour." },

    { q: "A query on a case-insensitive collation groups 'Rossi' and 'rossi' together. Is that a display issue?",
      a: ["Yes, only display",
          "No — collation decides equality, so it affects GROUP BY, UNIQUE, DISTINCT and which rows come back",
          "Only in SQL Server",
          "Only for indexes"],
      c: 1,
      why: "Which is why a collation change is a behaviour change to every query in the database, and should be decided once at creation." },

    { q: "What is the practical value of a calendar (dim_date) table?",
      a: ["It speeds up date arithmetic",
          "Working days and public holidays become data you can join to, rather than logic you must reimplement in every query",
          "It replaces the DATE type",
          "It is required for partitioning"],
      c: 1,
      why: "'Sales per working day' and 'fill in the days with no orders' become ordinary joins. It is also the only sane place to encode that Ferragosto exists." },

    { q: "In SQL Server, <code>placed_at AT TIME ZONE 'UTC' AT TIME ZONE 'W. Europe Standard Time'</code> — why two clauses?",
      a: ["It is a typo",
          "The first declares what the stored value IS, the second converts it to the target zone",
          "For daylight saving",
          "One is for the date, one for the time"],
      c: 1,
      why: "A naive datetime carries no zone, so you must tell the engine how to interpret it before converting. Note the Windows zone names differ from IANA names." },

    { q: "Which normalisation is standard before deduplicating an imported email column?",
      a: ["UPPER(email)",
          "NULLIF(TRIM(LOWER(email)), '')",
          "REPLACE(email, ' ', '')",
          "CAST(email AS VARCHAR(200))"],
      c: 1,
      why: "Trim whitespace, fold case, and turn the empty string — the classic import artefact — into a proper NULL. Then deduplicate on the normalised value, not the raw one." },

    { q: "Why does <code>WHERE created_at &gt; NOW() - INTERVAL '30 days'</code> remain sargable?",
      a: ["It does not",
          "Because the function is applied to the constant side, not to the indexed column",
          "Because NOW() is cached",
          "Because INTERVAL is special"],
      c: 1,
      why: "The rule is only about the column. Any amount of computation on the other side of the operator is fine — it is evaluated once and produces a boundary value." }
  ],

  /* --------------------------------------------------- 18 · JSON in SQL -- */
  "18-json-in-sql": [
    { q: "Which is a good reason to use a JSON column?",
      a: ["To avoid designing a schema",
          "Genuinely open-ended per-tenant attributes, a payload kept verbatim, or sparse settings read as a unit",
          "To store foreign keys flexibly",
          "To make queries faster"],
      c: 1,
      why: "And the case where it is wrong: a field you filter, join or aggregate on. That is a column." },

    { q: "What do you lose by putting a field in a JSON column instead of a real column?",
      a: ["Nothing important",
          "Type checking, NOT NULL, uniqueness, foreign keys and column statistics — so the engine can neither prevent bad data nor plan well",
          "Only the ability to index it",
          "Only readability"],
      c: 1,
      why: "Which are the two reasons for choosing a relational database in the first place." },

    { q: "In PostgreSQL, why prefer <code>jsonb</code> to <code>json</code>?",
      a: ["It preserves key order",
          "It stores a parsed binary form: faster to read, and the only one that indexes usefully",
          "It uses less space",
          "It is newer"],
      c: 1,
      why: "json stores the exact text and re-parses on every access. If you genuinely need the original bytes, a text column is more honest about what you are doing." },

    { q: "How do you index a specific JSON path in SQL Server?",
      a: ["CREATE JSON INDEX",
          "Add a PERSISTED computed column using JSON_VALUE and index that",
          "A GIN index",
          "SQL Server cannot index JSON at all"],
      c: 1,
      why: "SQL Server has no native JSON index type. PostgreSQL offers both a GIN index over the whole document and — usually better — a B-tree index on one extracted expression." },

    { q: "What is the difference between JSON_VALUE and JSON_QUERY in SQL Server?",
      a: ["None",
          "JSON_VALUE extracts a scalar; JSON_QUERY extracts an object or array",
          "JSON_QUERY is faster",
          "JSON_VALUE validates the document"],
      c: 1,
      why: "Using the wrong one returns NULL rather than an error, which is a quietly annoying way to lose an afternoon." },

    { q: "What does PostgreSQL's <code>@&gt;</code> operator do on jsonb?",
      a: ["Extracts a key",
          "Tests containment — whether the left document contains the right one",
          "Concatenates documents",
          "Compares for equality"],
      c: 1,
      why: "It is the operator a GIN index accelerates, which is why containment queries are the case where a whole-document index earns its cost." },

    { q: "What is the recommended pattern when several JSON paths need indexing?",
      a: ["Add a GIN index over the document",
          "Promote those fields into real (generated/computed) columns with ordinary indexes, keeping the document for the rest",
          "Split the JSON across several columns",
          "Move to a document database"],
      c: 1,
      why: "You get constraints, types, statistics and normal indexes on the parts that matter. Three indexes on three JSON paths is a schema asking to be written." },

    { q: "How do you turn a JSON array into rows in SQL Server?",
      a: ["JSON_QUERY", "OPENJSON with CROSS APPLY", "STRING_SPLIT", "A recursive CTE"],
      c: 1,
      why: "OPENJSON with an explicit WITH schema is also the standard way to bulk-load a JSON payload into a table in one statement, which is far better than a loop." },

    { q: "A PostgreSQL generated column <code>email TEXT GENERATED ALWAYS AS (data -&gt;&gt; 'email') STORED</code> gives you what?",
      a: ["A copy that can drift from the document",
          "A typed, indexable column always in step with the JSON, with one source of truth",
          "A view",
          "A trigger"],
      c: 1,
      why: "The engine maintains it, so there is no synchronisation code to get wrong — the concern that makes hand-maintained denormalised copies risky." },

    { q: "Which statement about JSON in a relational database is most defensible in an interview?",
      a: ["JSON columns should never be used",
          "It is right for genuinely schemaless parts of a record, and anything you filter on should be promoted to a real column",
          "Everything should be JSON for flexibility",
          "JSON is as fast as normal columns"],
      c: 1,
      why: "It shows you know both the legitimate use and the cost, which is what the question is really probing." }
  ],

  /* ================================================================= PART 0
     From absolute zero. Ten chapters, appended to this file rather than given
     one of their own, because loadQuizzes() and every page's script list read
     exactly three banks — and a fourth file would be a contract change in
     eleven places to hold questions that fit here perfectly well.        */

  /* -------------------------------------------- 0A · what a database is -- */
  "00a-what-is-a-database": [
    { q: "What does a database give you that a spreadsheet does not?",
      a: ["Prettier charts",
          "Concurrent access, all-or-nothing changes, rules that cannot be bypassed, and size beyond memory",
          "The ability to store text and numbers",
          "Formulas"],
      c: 1,
      why: "Those four are the whole justification. For a one-off analysis by one person a spreadsheet is often the correct answer, and saying so is a stronger response than reciting database features." },

    { q: "What does it mean that SQL is a <em>declarative</em> language?",
      a: ["It is compiled rather than interpreted",
          "You describe the result you want, and the engine decides how to produce it",
          "It has no loops",
          "Statements must be declared before use"],
      c: 1,
      why: "It matters because the engine can choose a faster plan next year — after an index is added or the data grows — without anybody rewriting the query." },

    { q: "In database terms, what is an <em>engine</em>?",
      a: ["The program that owns the data files and executes statements",
          "The program you type queries into",
          "The network protocol",
          "The query optimiser"],
      c: 0,
      why: "SQL Server is an engine; SSMS is a client. Confusing the two is why 'I installed SSMS but there is no database' is such a common first question." },

    { q: "Which four properties does a transaction guarantee?",
      a: ["Speed, safety, size, simplicity",
          "Atomicity, consistency, isolation, durability",
          "Availability, consistency, partition tolerance, latency",
          "Accuracy, completeness, integrity, durability"],
      c: 1,
      why: "ACID. Atomicity is the one that stops half-applied work; durability is the one that survives the power failing." },

    { q: "A colleague says “we should move this reporting spreadsheet into the database”. What is the strongest thing to ask first?",
      a: ["Which engine we should use",
          "Whether more than one person writes to it, and whether a half-finished edit would matter",
          "How many rows it has",
          "Whether we have a licence"],
      c: 1,
      why: "Those two questions decide whether a database is genuinely needed. Row count matters much later, and 'this could stay a spreadsheet' is sometimes the senior answer." },

    { q: "Roughly how much of what you write in SQL is portable between the major engines?",
      a: ["About 15%", "About half", "About 85%", "All of it — SQL is a standard"],
      c: 2,
      why: "The relational core is the same everywhere; the differences are row limits, date functions, string functions and procedural syntax. Chapter 44 is that lookup table." },

    { q: "Which engine does the Italian job market most often name in SQL developer adverts?",
      a: ["PostgreSQL", "MySQL", "SQL Server", "MongoDB"],
      c: 2,
      why: "SQL Server first, Oracle in larger and older estates, PostgreSQL and MySQL in newer product teams. Learn the one your target employer runs — and say which one you know deeply." },

    { q: "What distinguishes a database developer from a DBA?",
      a: ["Seniority",
          "The developer designs schemas and writes and tunes queries; the DBA installs, backs up, monitors and secures the server",
          "The DBA writes SQL and the developer does not",
          "Nothing — the titles are interchangeable"],
      c: 1,
      why: "One lives inside the data, the other around it. In a small company one person may be both, and saying which you want to be is a better interview answer than claiming both." },

    { q: "Why is “the database refuses the write” a feature rather than an obstacle?",
      a: ["It makes the application simpler to write",
          "Because a rule enforced by the engine cannot be bypassed by an application bug, a script, or somebody in a console",
          "Because it is faster than checking in code",
          "It is not a feature — it is a limitation"],
      c: 1,
      why: "That is the whole argument for constraints. Validation in one application protects that application; a constraint protects the data from every application, including the ones not written yet." },

    { q: "What is the relationship between a table and a result set?",
      a: ["A result set is a table: unnamed and temporary, but composable",
          "A result set is a list, not a table",
          "A result set is stored on disk",
          "They are unrelated concepts"],
      c: 0,
      why: "It is why queries can be nested, why a view can be used like a table, and why you never learn a second mechanism for 'a thing that produces rows'." },

    { q: "Where does this course's playground run its SQL engine?",
      a: ["On a server, over the network",
          "In your browser: a real SQLite engine compiled to WebAssembly, served from the site folder",
          "It simulates results without an engine",
          "In a Docker container you must start first"],
      c: 1,
      why: "Nothing is sent anywhere and it works offline, which is why every runnable example in the course carries a 'Run this' link." }
  ],

  /* ---------------------------------------- 0B · getting a database up --- */
  "00b-run-your-first-query": [
    { q: "What is the difference between a database engine and a database client?",
      a: ["None — they are two words for the same program",
          "The engine stores the data and executes statements; the client is the program you type into and read results from",
          "The client stores the data locally",
          "The engine is the GUI"],
      c: 1,
      why: "SSMS, DBeaver and psql are clients. SQL Server and PostgreSQL are engines. Installing a client gives you no database at all." },

    { q: "What is a SQLite database, physically?",
      a: ["A directory of files managed by a server process",
          "A single file, with no server process at all",
          "A cloud service",
          "A block of shared memory"],
      c: 1,
      why: "You can copy it, email it or delete it. It also means opening a filename that does not exist silently creates a new empty database rather than reporting an error." },

    { q: "Why does this course tell you to run Docker containers with <code>--rm</code>?",
      a: ["It makes the container start faster",
          "It destroys the container on exit, so a mistake while learning costs nothing",
          "It is required for port publishing",
          "It enables persistence"],
      c: 1,
      why: "Exactly what you want for learning, and exactly what you do not want for anything you care about keeping." },

    { q: "Which chapters genuinely cannot be demonstrated in SQLite?",
      a: ["Joins and aggregation",
          "Isolation levels and locking, because SQLite has a single writer",
          "Window functions",
          "Constraints"],
      c: 1,
      why: "With one writer there is no blocking to observe and no isolation anomalies to reproduce. Those chapters say so and give the Docker one-liner for an engine that can." },

    { q: "Most database clients run in <em>autocommit</em>. What does that mean for a mistaken <code>DELETE</code>?",
      a: ["It can be undone with Ctrl+Z",
          "It is already permanent by the time you read the row count",
          "It waits for confirmation",
          "It is rolled back automatically if it affects too many rows"],
      c: 1,
      why: "Which is why chapter 0H teaches the BEGIN … check the count … ROLLBACK-or-COMMIT ritual before you ever run a delete you care about." },

    { q: "Why does this course keep the schema and the seed data in separate files?",
      a: ["To make the download smaller",
          "So the structure can be rebuilt from scratch independently of the example data — which while learning you will want often",
          "Because SQLite requires it",
          "To support multiple engines"],
      c: 1,
      why: "The seed is also deterministic: it produces the same rows every time, so an answer you worked out yesterday is still the right answer today." },

    { q: "A Docker command fails with “port is already allocated”. What is the usual fix?",
      a: ["Reinstall Docker",
          "Publish a different host port, for example <code>-p 5433:5432</code>, and connect to that",
          "Restart the machine",
          "Use a different image"],
      c: 1,
      why: "Something else — often a previous container — already holds the port. The left number is the host port and only it has to change." },

    { q: "Why pin the engine's major version in a container image tag?",
      a: ["Smaller image",
          "Because testing on one version and deploying to another is testing a different product",
          "It is required by Docker",
          "Latest is never available"],
      c: 1,
      why: "The same argument appears in chapter 37 for CI: a test suite that passes on PostgreSQL 17 and deploys to 13 has tested the wrong engine." },

    { q: "In SQLite, what is the difference between <code>.tables</code> and <code>SELECT * FROM t;</code>?",
      a: ["None",
          "<code>.tables</code> is a command of the sqlite3 tool, not SQL — which is why it takes no semicolon",
          "<code>.tables</code> is faster",
          "<code>.tables</code> is deprecated SQL"],
      c: 1,
      why: "Dot-commands belong to the client. Pasting one into an application's SQL connection produces a syntax error, which surprises people once." },

    { q: "The playground reports that it could not start the SQL engine. What is the most likely cause?",
      a: ["The database file is missing",
          "WebAssembly is blocked, or the page was opened from file:// in a browser that refuses local .wasm",
          "The network is down",
          "The browser is too new"],
      c: 1,
      why: "The fix is to serve the folder over HTTP — <code>node tools/serve.mjs</code> — rather than opening the file directly." },

    { q: "Why is “nobody is hired for installing a database” still followed by a whole chapter about it?",
      a: ["Because installation is secretly the hard part",
          "Because the commonest reason beginners stop is that nothing ever actually ran",
          "Because interviewers ask about installation",
          "To pad the course"],
      c: 1,
      why: "A course you cannot run is a book. Ten minutes removing that failure mode is worth more than any amount of reading." }
  ],

  /* ----------------------------------- 0C · tables, rows, columns -------- */
  "00c-tables-rows-columns": [
    { q: "What does it mean that a table is a <em>set</em> of rows?",
      a: ["It can hold only unique values",
          "The rows have no inherent order, so a query without ORDER BY may return them in any order",
          "It is limited in size",
          "It cannot be modified"],
      c: 1,
      why: "Insertion order is not a promise. It usually looks stable, which is exactly why relying on it fails in production rather than in testing." },

    { q: "What is the difference between a primary key and a unique constraint?",
      a: ["None",
          "A primary key is the row's identity — one per table, never NULL, and what other tables reference; a unique constraint just forbids duplicate values and there may be several",
          "A unique constraint is faster",
          "A primary key may be NULL"],
      c: 1,
      why: "Practically: <code>id</code> is the primary key and <code>email</code> gets a unique constraint. Most engines also allow many NULLs in a unique column, and SQL Server allows exactly one." },

    { q: "Why is a surrogate key usually preferred as the primary key?",
      a: ["It is faster to type",
          "It is meaningless, so it never has to change — unlike an email address or a VAT number",
          "It uses less disk",
          "Natural keys are not allowed"],
      c: 1,
      why: "The right answer is usually both: a surrogate key for the plumbing, and a UNIQUE constraint on the natural key so the business rule is still enforced." },

    { q: "What does a <code>FOREIGN KEY</code> constraint actually guarantee?",
      a: ["That the referenced row exists, so an order cannot point at a customer who is not there",
          "That the join will be fast",
          "That the values are unique",
          "That deletes cascade"],
      c: 0,
      why: "It also prevents deleting a parent that still has children. Cascading is a separate, optional and much more dangerous choice." },

    { q: "In SQLite, what is unusual about foreign keys?",
      a: ["They cannot be declared",
          "They are OFF by default, per connection, so a schema's constraints may never be enforced",
          "They are always enforced",
          "They require an index"],
      c: 1,
      why: "<code>PRAGMA foreign_keys = ON;</code> per connection. A schema whose constraints are never enforced looks identical to one that has none." },

    { q: "Why is it better to store a customer's city once and point at it, rather than on every order?",
      a: ["It uses less disk",
          "Because one fact stored many times must be corrected many times, and missing one leaves the database holding two answers",
          "Because joins are fast",
          "Because normalisation is required by the standard"],
      c: 1,
      why: "That is an update anomaly, and removing it is what normalisation is for. The cost is a join, and that trade is usually worth making." },

    { q: "How does the database describe its own structure?",
      a: ["In a text file on disk",
          "In tables: the catalogue — <code>information_schema</code> or the engine's own views — is queryable with ordinary SQL",
          "It does not",
          "Only through a GUI"],
      c: 1,
      why: "Which is why chapter 37 can write a test that asserts every foreign key has an index: it is a query over the catalogue, so it can never be out of date." },

    { q: "A result set has a column header reading <code>?column?</code>. What happened?",
      a: ["The query failed",
          "A computed column was selected without giving it an alias",
          "The column type is unknown",
          "The client is misconfigured"],
      c: 1,
      why: "Expressions have no name of their own. Without <code>AS eur</code>, an application reading the result by column name breaks." },

    { q: "What is the practical consequence of a result set being a table?",
      a: ["It can be saved to disk",
          "It composes: a query can select from another query, which is why subqueries, views and CTEs all work the same way",
          "It has a primary key",
          "It is unordered"],
      c: 1,
      why: "You never learn a second mechanism for 'a thing that produces rows'. There is one, and it is the table." },

    { q: "What does <code>NULL</code> mean in a column?",
      a: ["Zero", "An empty string", "The value is absent or unknown", "An error occurred"],
      c: 2,
      why: "It is not zero and not an empty string. Arithmetic on it yields NULL and comparison to it yields UNKNOWN, which is chapter 09's entire subject." },

    { q: "Why does every table need a primary key?",
      a: ["The standard requires it",
          "Without one, two identical rows cannot be told apart, addressed or corrected individually",
          "For performance only",
          "So it can be joined"],
      c: 1,
      why: "Chapter 15 shows the clean-up when a table has no key: deleting duplicates by physical row address, which is a genuinely last-resort technique." }
  ],

  /* ------------------------------------------- 0D · SELECT and FROM ------ */
  "00d-select-and-from": [
    { q: "In <code>SELECT name FROM customers;</code>, which part does the engine resolve first?",
      a: ["SELECT", "FROM", "They are resolved together", "It depends on the engine"],
      c: 1,
      why: "SELECT cannot mean anything until the engine knows which columns exist, so FROM is resolved first. That inversion explains most beginner errors — chapter 06 is devoted to it." },

    { q: "Why is <code>SELECT *</code> discouraged in code that will be kept?",
      a: ["It is slower to parse",
          "It is expanded at parse time, so the result changes silently when the table does — and it can never be served by a narrow covering index",
          "It is not valid standard SQL",
          "It returns rows in the wrong order"],
      c: 1,
      why: "It also fetches columns nobody asked for and hides the query's intent. At an interactive prompt, while exploring, it is exactly the right tool." },

    { q: "What does <code>AS</code> do in <code>SELECT total_cents / 100.0 AS eur</code>?",
      a: ["Converts the type",
          "Names the computed column in the result; the table itself is unchanged",
          "Creates a new column in the table",
          "Rounds the value"],
      c: 1,
      why: "Without it the header is engine-specific rubbish, and an application reading the result by column name breaks." },

    { q: "What is the difference between <code>'name'</code> and <code>\"name\"</code> in a query?",
      a: ["None — both are strings",
          "Single quotes make a text literal; double quotes make a quoted identifier such as a column name",
          "Double quotes are for numbers",
          "Single quotes are only for dates"],
      c: 1,
      why: "So <code>WHERE city = 'Modena'</code> compares against text, while <code>WHERE \"city\" = Modena</code> tries to compare two columns. SQL Server also accepts [brackets] for identifiers." },

    { q: "What does <code>DISTINCT</code> apply to?",
      a: ["The first column only",
          "The whole row of the result, so with two columns it returns distinct pairs",
          "Each column independently",
          "Only numeric columns"],
      c: 1,
      why: "Also worth remembering: it costs a sort or a hash, and later in the course DISTINCT added to remove duplicates is usually a symptom of a join fan-out." },

    { q: "<code>SELECT name email FROM customers;</code> runs without error. What did it do?",
      a: ["Returned two columns",
          "Returned one column of names, aliased 'email', because the comma was forgotten and AS is optional",
          "Concatenated the two columns",
          "Raised a warning"],
      c: 1,
      why: "This is the strongest practical argument for always writing AS: a missing comma becomes a silently wrong result instead of a syntax error." },

    { q: "The engine reports “no such column: emial”. What does that almost always mean?",
      a: ["A permissions problem",
          "Exactly what it says: a typo, or the column is on a table you have not joined to",
          "The table is empty",
          "The connection was lost"],
      c: 1,
      why: "Database error messages are unusually literal, and the position they report is where the parser gave up — often one token after the real mistake." },

    { q: "Why does this course write keywords in capitals when the engine does not care?",
      a: ["It is required by the standard",
          "So the clause structure of a statement is visible at a glance",
          "Lower case is slower to parse",
          "For compatibility with older engines"],
      c: 1,
      why: "Together with one column per line and keywords starting the line, it makes the query's shape — and therefore its evaluation order — readable without reading the words." },

    { q: "What is the point of aliasing a table as <code>FROM customers AS c</code> when there is only one table?",
      a: ["It is required",
          "It is a habit that pays off the moment there are two: qualifying every column makes a multi-table query readable and unambiguous",
          "It makes the query faster",
          "It renames the table"],
      c: 1,
      why: "With two tables that both have an <code>id</code>, qualification stops being politeness and becomes mandatory." },

    { q: "Does whitespace or line layout change how a query executes?",
      a: ["Yes, indentation is significant",
          "No — the layout is entirely for people",
          "Only inside string literals",
          "Only in stored procedures"],
      c: 1,
      why: "Which is why the layout convention is worth adopting for free: it costs the engine nothing and makes review possible." },

    { q: "When is <code>SELECT *</code> the right thing to write?",
      a: ["Never",
          "While exploring an unfamiliar table at a prompt, where seeing every column is the point",
          "In views",
          "In application code, for flexibility"],
      c: 1,
      why: "The rule is about code that will be kept. A rule you apply without exception is one you have stopped thinking about." }
  ]
,

  /* ------------------------------------------------ 0E · WHERE ---------- */
  "00e-where-filtering": [
    { q: "A <code>WHERE</code> clause keeps a row when its condition evaluates to what?",
      a: ["Anything that is not FALSE", "TRUE only", "TRUE or UNKNOWN", "Anything that is not NULL"],
      c: 1,
      why: "SQL has three truth values, and UNKNOWN is discarded exactly like FALSE. That single rule is behind almost every NULL surprise in the language." },

    { q: "<code>WHERE status = 'shipped' OR status = 'delivered' AND total_cents &gt; 50000</code> — what does it actually mean?",
      a: ["Both statuses, each over €500",
          "'shipped' with any total, OR 'delivered' over €500 — because AND binds tighter than OR",
          "A syntax error",
          "Either status, over €500"],
      c: 1,
      why: "The habit that removes the question: bracket every mixture of AND and OR, even when you are sure. Two characters against a whole class of silent bug." },

    { q: "Why is <code>BETWEEN</code> a trap on a timestamp column?",
      a: ["It is slower than >= and <",
          "It is inclusive at both ends, so BETWEEN '2026-01-01' AND '2026-01-31' loses everything after midnight on the 31st",
          "It cannot use an index",
          "It does not work on dates"],
      c: 1,
      why: "The half-open range — <code>&gt;= start AND &lt; next_start</code> — is correct at any precision, including one the column gains in a later migration." },

    { q: "Why can <code>LIKE 'Rossi%'</code> use an index while <code>LIKE '%rossi%'</code> cannot?",
      a: ["The second is case-insensitive",
          "An index is ordered by prefix, and the second pattern gives no prefix to start from",
          "The second uses two wildcards",
          "Neither can use an index"],
      c: 1,
      why: "Think of a phone book: 'surnames starting Rossi' is a place you can open it; 'surnames containing rossi' means reading every page. The real fix for infix search is full-text search." },

    { q: "Why does <code>WHERE shipped_at = NULL</code> return no rows at all?",
      a: ["It is a syntax error",
          "Comparison with NULL yields UNKNOWN rather than TRUE, and WHERE keeps only TRUE",
          "shipped_at is never null",
          "The engine optimises it away"],
      c: 1,
      why: "And it is not an error, which is what makes it dangerous: you get a plausible empty result. Only IS NULL and IS NOT NULL can test for absence." },

    { q: "<code>WHERE status &lt;&gt; 'cancelled'</code> — which rows does it silently exclude?",
      a: ["None",
          "Rows where status is NULL, because NULL &lt;&gt; 'cancelled' is UNKNOWN",
          "Rows where status is an empty string",
          "Cancelled rows only"],
      c: 1,
      why: "So two queries that look like opposites can both miss the same rows. The query-level fix is <code>OR status IS NULL</code>; the schema-level fix is NOT NULL, and it is much better." },

    { q: "What is the advantage of <code>status IN ('shipped','delivered')</code> over two ORs?",
      a: ["It is faster in every case",
          "It is clearer, harder to misread, and the optimiser recognises it as a set of seeks",
          "It handles NULL differently",
          "It avoids brackets"],
      c: 1,
      why: "The negative form is a different story: NOT IN over a nullable subquery returns nothing at all — chapter 09." },

    { q: "Which of these predicates can use an index on <code>placed_at</code>?",
      a: ["<code>WHERE YEAR(placed_at) = 2026</code>",
          "<code>WHERE placed_at &gt;= '2026-01-01' AND placed_at &lt; '2027-01-01'</code>",
          "<code>WHERE CAST(placed_at AS DATE) = '2026-03-14'</code>",
          "All of them"],
      c: 1,
      why: "The index stores placed_at, not any function of it. Keep the column bare on one side of the comparison — that is sargability, and chapter 07 is the full treatment." },

    { q: "What does the word <em>sargable</em> mean?",
      a: ["Safe against injection",
          "Search-argument-able: the predicate can be used to seek in an index rather than forcing a scan",
          "Aggregatable",
          "Sortable"],
      c: 1,
      why: "Using it correctly in an interview is a genuine signal, because it means you have thought about how the filter reaches the data rather than only about what it returns." },

    { q: "Both sides of a comparison must be comparable. What happens when a text column is compared to a numeric literal?",
      a: ["A syntax error",
          "The engine converts one side — usually the column, once per row, which disables the index",
          "The literal is converted to text",
          "The comparison is skipped"],
      c: 1,
      why: "Numeric types outrank string types in precedence, so the text column loses. The result is correct and a hundred times slower, which is why it survives code review." },

    { q: "Why is a query with no <code>WHERE</code> clause a performance question as well as a correctness one?",
      a: ["It is not",
          "Because it reads the whole table, so saying what you actually want is the first optimisation you ever make",
          "Because it locks the table",
          "Because it returns duplicates"],
      c: 1,
      why: "Filtering early is the theme the rest of the course develops: WHERE runs before grouping, and an index can turn the filter into a seek." }
  ],

  /* ------------------------------------- 0F · ORDER BY and LIMIT --------- */
  "00f-order-by-and-limit": [
    { q: "In what order does a query without <code>ORDER BY</code> return its rows?",
      a: ["Insertion order", "Primary key order", "Undefined — whatever the chosen access path produced", "Random"],
      c: 2,
      why: "It usually looks stable, which is exactly why relying on it fails in production rather than in testing. Add an index or run it on a replica and the order can change." },

    { q: "What does the second column in <code>ORDER BY placed_at DESC, id DESC</code> do?",
      a: ["Sorts the result twice",
          "Breaks ties: it only decides the order of rows equal on placed_at",
          "Groups the rows",
          "Nothing, unless placed_at is NULL"],
      c: 1,
      why: "And when the tie-breaker is unique — the primary key — the total order is fixed, which is what makes paging repeatable." },

    { q: "A paginated list shows the same row on two pages. What is the most likely cause?",
      a: ["A caching bug",
          "The ORDER BY has ties and no unique tie-breaker, so tied rows may be ordered differently for each page",
          "The LIMIT is wrong",
          "The index is missing"],
      c: 1,
      why: "Append the primary key to the sort. If rows are also being inserted while the user pages, offset paging shifts everything too — keyset pagination fixes both." },

    { q: "Which spelling of “the first ten rows” belongs to SQL Server?",
      a: ["<code>LIMIT 10</code>",
          "<code>TOP (10)</code>, written before the column list",
          "<code>FETCH FIRST 10 ROWS ONLY</code>",
          "<code>ROWNUM &lt;= 10</code>"],
      c: 1,
      why: "LIMIT is PostgreSQL/MySQL/SQLite, FETCH FIRST is the standard and Oracle 12c+, and ROWNUM is Oracle's legacy trap because it is assigned before ORDER BY runs." },

    { q: "Why does page 500 of an <code>OFFSET</code>-paginated list take so much longer than page 1?",
      a: ["The rows are further away on disk",
          "The engine must produce and discard every skipped row before returning any",
          "The index is only used for the first page",
          "It does not — the cost is constant"],
      c: 1,
      why: "The cost is proportional to how deep the page is, and everything counted is thrown away. Keyset pagination — chapter 30 — makes it constant." },

    { q: "Why can <code>ORDER BY</code> use a column alias when <code>WHERE</code> cannot?",
      a: ["It is a special case in the standard",
          "ORDER BY runs after SELECT, which is where the alias is created; WHERE runs before it",
          "WHERE could, but engines forbid it",
          "Aliases only work on computed columns"],
      c: 1,
      why: "It is the clearest everyday consequence of SQL's evaluation order, and chapter 06 turns it into a general rule." },

    { q: "A column of numbers sorts as 1, 10, 2, 3. What is wrong?",
      a: ["The ORDER BY is missing a direction",
          "The column's type is text, so it sorts as text",
          "The index is corrupt",
          "The collation is wrong"],
      c: 1,
      why: "Check the type before anything else. It is also why ISO-8601 dates stored as text sort correctly — the format was designed so that lexical order matches chronological order." },

    { q: "Where do NULLs sort?",
      a: ["Always first", "Always last", "Together at one end, and which end differs by engine", "They are excluded from sorted results"],
      c: 2,
      why: "PostgreSQL puts them last ascending, SQL Server first. <code>NULLS FIRST</code>/<code>NULLS LAST</code> is the standard spelling, and SQL Server does not support it." },

    { q: "Does <code>LIMIT 10</code> mean the engine only reads ten rows?",
      a: ["Yes, always",
          "No — without an index matching the sort it must produce and sort everything to know which ten",
          "Only with an ORDER BY",
          "Only on small tables"],
      c: 1,
      why: "Which is why the plan for 'the ten largest orders' shows a full scan and a sort unless there is an index on that column." },

    { q: "Why is “the first ten” meaningless without an <code>ORDER BY</code>?",
      a: ["It is a syntax error",
          "Because the rows have no order, so 'the first ten' is 'any ten' and can differ between runs",
          "Because LIMIT requires a sort key",
          "It is not meaningless — insertion order applies"],
      c: 1,
      why: "The engine will happily give you a different ten tomorrow, after an index is added or the plan changes." },

    { q: "Which clause runs last in a <code>SELECT</code>?",
      a: ["ORDER BY", "The row limit — LIMIT / TOP / FETCH", "SELECT", "WHERE"],
      c: 1,
      why: "FROM, WHERE, GROUP BY, HAVING, SELECT, ORDER BY, then the limit. Knowing that sequence answers a surprising number of interview questions." }
  ],

  /* --------------------------------- 0G · expressions and types --------- */
  "00g-expressions-and-types": [
    { q: "<code>SELECT total_cents / 100 FROM orders</code> where total_cents is 12550. What comes back?",
      a: ["125.5", "125", "126", "An error"],
      c: 1,
      why: "Integer ÷ integer truncates, so the cents are lost. <code>/ 100.0</code> introduces a non-integer operand and keeps them. This is the most common wrong number in a beginner's first report." },

    { q: "How do you write an apostrophe inside a SQL text literal?",
      a: ["Escape it with a backslash", "Double it: <code>'L''Aquila'</code>", "Use double quotes instead", "It is not possible"],
      c: 1,
      why: "In application code you never do this by hand — you pass a parameter, and the question stops arising entirely." },

    { q: "What is the difference between <code>'2026-03-14'</code> and <code>DATE '2026-03-14'</code>?",
      a: ["None",
          "The first is text until something converts it, using the session's date format; the second is a typed literal that cannot be misread",
          "The second is faster",
          "The second is SQL Server only"],
      c: 1,
      why: "And ISO-8601 — YYYY-MM-DD — is the only format worth writing in either form, because it is unambiguous and sorts correctly as text." },

    { q: "What does <code>'Rossi' || NULL</code> produce?",
      a: ["'Rossi'", "NULL", "An empty string", "An error"],
      c: 1,
      why: "The classic symptom is an address label that is completely blank for everybody with no suite number. <code>CONCAT()</code> is defined to ignore NULLs, which is the tidier fix." },

    { q: "What does <code>CAST</code> do that an implicit conversion does not?",
      a: ["It is faster",
          "It is written by you, visible in review, and raises an error when the value will not convert",
          "It never fails",
          "It works on more types"],
      c: 1,
      why: "An implicit conversion is inserted by the engine following precedence rules you did not choose — and when it lands on an indexed column in a WHERE clause it silently turns a seek into a scan." },

    { q: "Where is a conversion free, and where does it cost you an index?",
      a: ["Both are free",
          "In the SELECT list it is free; on a column inside a WHERE clause it can disable the index",
          "Both cost the same",
          "It depends on the engine only"],
      c: 1,
      why: "Cast in the SELECT list, never on the column being filtered. That one sentence is most of what a junior needs to know about conversions." },

    { q: "In a <code>CASE</code> expression, what happens if no branch matches and there is no <code>ELSE</code>?",
      a: ["An error", "The result is NULL", "The first branch is used", "The row is skipped"],
      c: 1,
      why: "Which is how a CASE that looks exhaustive quietly produces nulls — and then the arithmetic downstream produces nulls too." },

    { q: "Why does branch order matter in a searched <code>CASE</code>?",
      a: ["It does not",
          "Branches are tested top to bottom and the first TRUE one wins, so a broader test placed first hides the narrower ones",
          "Only for performance",
          "Only when ELSE is omitted"],
      c: 1,
      why: "Swap 'over €1000' and 'over €200' and every large order is labelled medium, because 100000 is also ≥ 20000." },

    { q: "What is <code>NULLIF(total, 0)</code> for?",
      a: ["Replacing NULL with zero",
          "Turning 0 into NULL, which makes <code>x / NULLIF(y, 0)</code> return NULL instead of raising a divide-by-zero",
          "Testing for zero",
          "Counting nulls"],
      c: 1,
      why: "It is the idiomatic division guard, and interviewers check for it in the month-on-month-change question specifically." },

    { q: "Is <code>CASE</code> a statement or an expression?",
      a: ["A statement, like IF",
          "An expression: it produces a value, so it can appear anywhere a value can — including inside an aggregate",
          "Both, depending on the engine",
          "Neither"],
      c: 1,
      why: "That is what makes conditional aggregation — <code>COUNT(CASE WHEN … THEN 1 END)</code> — possible, which chapters 10 and 16 build on heavily." },

    { q: "What is the right way to learn SQL's function library?",
      a: ["Memorise the reference for your engine",
          "Learn a dozen that come up constantly and look the rest up, saying so plainly when asked",
          "Avoid functions",
          "Learn only the standard ones"],
      c: 1,
      why: "Interviewers ask what COALESCE does; they do not ask you to recite DATEADD's argument order. 'I would check the docs for the exact spelling' is a normal professional answer." }
  ]
,

  /* --------------------------------- 0H · CREATE, INSERT, UPDATE, DELETE  */
  "00h-create-insert-update-delete": [
    { q: "What is the difference between DDL and DML?",
      a: ["DDL is for developers, DML for administrators",
          "DDL changes the schema (CREATE, ALTER, DROP); DML changes the rows (INSERT, UPDATE, DELETE)",
          "DDL is faster",
          "DML cannot be rolled back"],
      c: 1,
      why: "And it is operational, not academic: in Oracle and MySQL a DDL statement commits the open transaction, so a CREATE in the middle of your careful transaction has already made everything before it permanent." },

    { q: "Why should an <code>INSERT</code> always name its columns?",
      a: ["It is required by the standard",
          "Without a column list the statement is positional, so adding a column to the table later breaks it — or fills the wrong columns",
          "It is faster",
          "To allow defaults"],
      c: 1,
      why: "Columns you do not name take their DEFAULT, or NULL where there is none — which is how <code>id</code> and <code>created_at</code> fill themselves in." },

    { q: "When is a column's <code>DEFAULT CURRENT_TIMESTAMP</code> evaluated?",
      a: ["When the table is created",
          "On every INSERT that does not supply the column, so each row gets its own moment",
          "When the database starts",
          "Once per transaction"],
      c: 1,
      why: "Store UTC while you are at it: in SQL Server that means SYSUTCDATETIME(), not GETDATE()." },

    { q: "What happens when you run <code>UPDATE notes SET priority = 1;</code> with no WHERE clause?",
      a: ["The engine asks for confirmation",
          "Every row in the table is updated, with no prompt and no undo",
          "It fails as unsafe",
          "Only the first row is updated"],
      c: 1,
      why: "The statement is valid SQL and the engine does exactly what it was told. The habit that prevents it — SELECT first, transaction always — costs fifteen seconds." },

    { q: "In the safe-delete ritual, what does the row count tell you?",
      a: ["How fast the statement ran",
          "Whether your predicate means what you thought: compare it with the count the SELECT showed",
          "How many rows are left",
          "Nothing useful"],
      c: 1,
      why: "412 where you expected 4 means the WHERE clause is not the one you intended — and inside a transaction, you can still find that out for free." },

    { q: "Does <code>ROLLBACK</code> undo everything a transaction did?",
      a: ["Yes, without exception",
          "It undoes the data changes, but identity or sequence values consumed are not given back — gaps are normal",
          "No, it only undoes the last statement",
          "Only in some engines"],
      c: 1,
      why: "A sequence is deliberately non-transactional, because making it transactional would serialise every insert. Gaps in an auto-numbered column are not corruption." },

    { q: "What is the difference between <code>DELETE</code> and <code>TRUNCATE</code>?",
      a: ["None — TRUNCATE is just shorter",
          "DELETE is DML: row by row, fully logged, filterable, rollback-able. TRUNCATE is DDL in most engines: it deallocates pages, is minimally logged, cannot be filtered and resets identity counters",
          "TRUNCATE deletes the table",
          "DELETE cannot be rolled back"],
      c: 1,
      why: "TRUNCATE is also usually refused while a foreign key references the table. On a large table the performance difference is enormous, which is why the question is asked so often." },

    { q: "Why does most client software leave you in <em>autocommit</em>, and why does it matter here?",
      a: ["It does not",
          "Because each statement is committed as it finishes, so without an explicit BEGIN a mistaken DELETE is permanent before you read the count",
          "Because transactions are slow",
          "Because it prevents deadlocks"],
      c: 1,
      why: "Knowing where your client's transaction control is, on day one, is the difference between a mistake and an incident." },

    { q: "A <code>CHECK</code> constraint is <code>CHECK (shipped_at &gt;= placed_at)</code> and shipped_at is NULL. Does the row pass?",
      a: ["No — NULL fails the check",
          "Yes — a constraint is only violated by a definite FALSE, and NULL makes it UNKNOWN",
          "It depends on the engine",
          "The insert errors"],
      c: 1,
      why: "Which is why a check about a nullable column has to spell out the IS NOT NULL half if you meant to require it." },

    { q: "What is the practical value of a <code>CHECK</code> constraint over validation in application code?",
      a: ["It is faster",
          "It cannot be bypassed — not by another application, a script, or somebody in a console at 2 a.m.",
          "It gives better error messages",
          "It is easier to change"],
      c: 1,
      why: "Validation in one application protects that application. A constraint protects the data from every application, including the ones not written yet." },

    { q: "Why is <code>INSERT … SELECT</code> preferable to a loop of single-row inserts from application code?",
      a: ["It is the only way to insert many rows",
          "One statement, one transaction, one round trip — instead of N of each",
          "It bypasses constraints",
          "It is required for large tables"],
      c: 1,
      why: "It is the write-side version of the N+1 problem, and it is the single most effective fix for a slow import." }
  ],

  /* ------------------------------------------- 0I · your first join ----- */
  "00i-your-first-join": [
    { q: "Why does an order store <code>customer_id</code> rather than the customer's name?",
      a: ["To save space",
          "So the name is stored once: correcting it is one update, and there is nowhere for a second spelling to survive",
          "Because names are not unique",
          "Because joins are fast"],
      c: 1,
      why: "Storing one fact many times creates an update anomaly. The cost of the alternative is a join, and that trade is usually worth making." },

    { q: "What does the <code>ON</code> clause of a join express?",
      a: ["Which rows to return",
          "The match rule: which row of one table belongs with which row of the other, almost always foreign key = primary key",
          "The sort order",
          "Which columns to show"],
      c: 1,
      why: "A JOIN with no ON is a cross product — every row paired with every row — so count your joins against your match rules when a result is unexpectedly enormous." },

    { q: "What is the difference between <code>INNER JOIN</code> and <code>LEFT JOIN</code>?",
      a: ["LEFT is faster",
          "INNER keeps only matched pairs; LEFT keeps every left row and pads the right-hand columns with NULL",
          "LEFT returns the left table only",
          "They are synonyms"],
      c: 1,
      why: "Choose by the question. 'Customers and their orders, if any' needs the left join — the 'if any' is the giveaway." },

    { q: "A date filter was moved from <code>ON</code> to <code>WHERE</code> and the customers with no orders vanished. Why?",
      a: ["The join type changed",
          "Padded rows carry NULL, the comparison is UNKNOWN, and WHERE discards them — so the outer join silently became an inner one",
          "WHERE cannot filter dates",
          "The index was lost"],
      c: 1,
      why: "The rule: if it is part of what counts as a match it belongs in ON; if it is a filter on the result it belongs in WHERE." },

    { q: "Why must columns be qualified — <code>o.id</code>, <code>c.id</code> — in a two-table join?",
      a: ["For readability only",
          "Because an unqualified column that exists on both tables is ambiguous, which is an error",
          "Because the standard requires it",
          "To help the optimiser"],
      c: 1,
      why: "With four tables it stops being politeness and becomes the difference between a query anyone can read and one that has to be reverse-engineered." },

    { q: "What is the effect of a <code>JOIN</code> written without an <code>ON</code> clause?",
      a: ["A syntax error in every engine",
          "A cross join: every row of A paired with every row of B",
          "An inner join on the primary keys",
          "The second table is ignored"],
      c: 1,
      why: "1800 orders and 300 customers gives 540 000 rows and looks like the query has hung. Some engines refuse it; others quietly obey." },

    { q: "In a chain <code>orders → customers → cities → regions</code>, why does the row count not grow?",
      a: ["Because of DISTINCT",
          "Because each step is many-to-one: an order has one customer, a customer one city",
          "Because of the WHERE clause",
          "It does grow"],
      c: 1,
      why: "Rows multiply when you join two one-to-many children of the same parent — that is the fan-out of chapter 08, and it is the thing to watch for." },

    { q: "Which columns become nullable in the result of a <code>LEFT JOIN</code>?",
      a: ["None",
          "Every column from the right-hand table, whatever its declared type",
          "Only the join key",
          "Only text columns"],
      c: 1,
      why: "That is where outer-join NULLs come from, and why COUNT(right.col) reports 0 for an unmatched left row while COUNT(*) reports 1." },

    { q: "Why does this course suggest rewriting a <code>RIGHT JOIN</code> as a <code>LEFT JOIN</code>?",
      a: ["RIGHT JOIN is slower",
          "It is the same operation mirrored, and reading joins consistently left-to-right makes review easier",
          "RIGHT JOIN is not standard",
          "It is not supported everywhere"],
      c: 1,
      why: "A style rule with a purpose: the preserved side is then always the one you read first." },

    { q: "When a query needs a column that its main table does not have, what does that tell you?",
      a: ["The schema is badly designed",
          "That the value lives in another table and a join is how you reach it — which is exactly what normalisation intends",
          "That the column should be duplicated",
          "That you need a subquery"],
      c: 1,
      why: "Reading it as a path — order → customer → city → region — is how a four-table query stays comprehensible." },

    { q: "<code>WHERE o.id IS NULL</code> after a <code>LEFT JOIN</code> — is that the mistake this chapter warns about?",
      a: ["Yes, always",
          "No: it deliberately keeps only the padded rows, which is how you ask 'which customers have no orders?'",
          "It is a syntax error",
          "Only if o.id is nullable"],
      c: 1,
      why: "It relies on o.id being NOT NULL in the table, so the only way it can be NULL in the result is the padding. Testing a nullable column instead makes the anti-join wrong." }
  ],

  /* ----------------------------------- 0J · your first GROUP BY --------- */
  "00j-your-first-group-by": [
    { q: "What does <code>GROUP BY customer_id</code> do to the number of rows in the result?",
      a: ["Nothing",
          "It collapses all the rows sharing a customer_id into one output row each",
          "It sorts them",
          "It removes duplicates"],
      c: 1,
      why: "It is the first query shape where output rows and input rows are different things, which is the step from filtering to reporting." },

    { q: "What is the difference between <code>COUNT(*)</code> and <code>COUNT(column)</code>?",
      a: ["None",
          "COUNT(*) counts rows; COUNT(column) counts rows where that column is not NULL",
          "COUNT(*) is faster",
          "COUNT(column) counts distinct values"],
      c: 1,
      why: "Across a LEFT JOIN the difference is decisive: COUNT(*) counts the null-padded row as 1, and COUNT(o.id) correctly reports 0." },

    { q: "Four rows hold 10, 20, NULL and 30. What does <code>AVG</code> return?",
      a: ["15", "20", "NULL", "An error"],
      c: 1,
      why: "60 ÷ 3, not 60 ÷ 4: the divisor is the count of non-null values. If NULL means 'nothing happened' in your data, the honest average needs AVG(COALESCE(x, 0))." },

    { q: "Why can you not select a bare, ungrouped column alongside an aggregate?",
      a: ["The standard forbids it for no good reason",
          "Because the group has many values for it and no rule for choosing one, so the engine refuses rather than guessing",
          "It is allowed everywhere",
          "Because of NULLs"],
      c: 1,
      why: "MySQL historically returned an arbitrary value instead of erroring, which is worse — it looks like it worked. Modern MySQL enables ONLY_FULL_GROUP_BY." },

    { q: "Where does a filter on <code>status</code> belong, and where does a filter on <code>COUNT(*)</code> belong?",
      a: ["Both in WHERE",
          "status in WHERE, COUNT(*) in HAVING",
          "Both in HAVING",
          "status in HAVING, COUNT(*) in WHERE"],
      c: 1,
      why: "Anything decidable from one row goes in WHERE — it runs first, shrinks the data and can use an index. Anything needing the whole group goes in HAVING." },

    { q: "Why is <code>WHERE COUNT(*) &gt; 3</code> a syntax error?",
      a: ["COUNT needs a column",
          "WHERE runs before GROUP BY, so at that point no groups exist for COUNT to count",
          "It should be COUNT(1)",
          "It is valid in most engines"],
      c: 1,
      why: "Filtering on an aggregate is exactly what HAVING exists for, and it runs immediately after the grouping." },

    { q: "A “orders per customer” report is missing customers who have never ordered. Why?",
      a: ["A WHERE clause is excluding them",
          "They have no rows in orders, so no group exists for them — the fix is a LEFT JOIN from customers",
          "GROUP BY removes them",
          "COUNT(*) skips them"],
      c: 1,
      why: "One of the most common reporting bugs there is: a group with no rows does not exist, so the customer is simply absent rather than showing zero." },

    { q: "What does <code>SUM</code> return over a group where every value is NULL, or over no rows at all?",
      a: ["0", "NULL", "An error", "The count"],
      c: 1,
      why: "Every aggregate except COUNT returns NULL over an empty set, which is why a dashboard total should be COALESCE(SUM(x), 0)." },

    { q: "<code>MAX(placed_at)</code> gives the latest date in each group. Does it give the rest of that row?",
      a: ["Yes",
          "No — it returns one value; 'the whole row with the latest date' is a different problem, needing a window function",
          "Only with GROUP BY",
          "Only in SQL Server"],
      c: 1,
      why: "That distinction is the bridge to chapter 13, where ROW_NUMBER() OVER (PARTITION BY … ORDER BY … DESC) picks the row rather than the value." },

    { q: "Why does <code>GROUP BY c.name</code> deserve a second look?",
      a: ["It is slower than grouping by id",
          "Two different customers who share a name would be merged into one row; GROUP BY c.id, c.name is the honest version",
          "Names cannot be grouped",
          "It requires an index"],
      c: 1,
      why: "A small thing, and exactly the kind of detail an interviewer is listening for when they ask you to walk through a reporting query." },

    { q: "In which order does the engine run <code>FROM</code>, <code>WHERE</code>, <code>GROUP BY</code>, <code>HAVING</code>, <code>SELECT</code>, <code>ORDER BY</code>?",
      a: ["In the order they are written",
          "FROM, WHERE, GROUP BY, HAVING, SELECT, ORDER BY",
          "SELECT first, then the rest",
          "It varies by engine"],
      c: 1,
      why: "That sequence explains why HAVING can see aggregates and WHERE cannot, and why ORDER BY alone can use a SELECT alias. Chapter 06 is the full argument." }
  ]

});
